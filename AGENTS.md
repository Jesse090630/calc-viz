# AGENTS.md — 接手这个项目前必读

> 这是给 AI 编码代理(Codex / Claude Code / 其他)的上下文锚点。
> 每次新会话先读这个文件,再读 `docs/HANDOFF.md` 拿工作队列。
> 本文件里的规则**优先于你的默认习惯**。与之冲突时,以本文件为准。

---

## 0. 三十秒版本

做一个 **Calculus 推导过程播放器**。不是计算器,不是画图工具。

每个概念 = 一条 **derivation chain**,8–9 步。用户按 → 键前进,每一步屏幕上的**几何动作**和**公式变化**同时发生,且严格一一对应。产品回答的是 **"这个公式是从哪来的"**,不是 **"这个公式是什么"**。

- 线上(**唯一在跑的**):https://jesse090630.github.io/calc-viz/
  ⚠️ https://calcviz.netlify.app **不要当成线上站** —— 它停在 2026-08-23 的构建上,
  既没连仓库、也部署不了(账户额度用尽)。详见第 6 节。
- 仓库:https://github.com/Jesse090630/calc-viz (public, `main`)
- 目标用户:正在学 AP Calculus AB/BC 的高中生
- 项目所有者 Jesse **不写代码**。他是产品经理 + 验收测试员。你写全部代码。

### 差异点(做任何取舍时回来看这一条)
GeoGebra / Desmos 3D / 各种 volume calculator 都只**显示结果**。
没有一个展示**公式的诞生过程**且**可交互**。
**任何让产品更像计算器的功能请求,默认拒绝。**

---

## 1. 三个禁止(硬性,由测试强制)

**禁止 1** — `src/math/**` 不得 import 任何渲染库(`three` / `react` / `@react-three/*` / `zustand` / `katex`)。
理由:数学正确性必须能用 `npm test` 单独验证,不能依赖"打开网页看一眼"。

**禁止 2** — `src/concepts/**` 与所有组件里不得出现裸数学算式。
看到自己在组件里写 `2 * Math.PI * x * f(x)` 就是违规,必须放进 `src/math/` 并从那里调用。

**禁止 3** — `src/engine/**` 不得出现任何具体概念的名字(`shell` / `washer` / `disk` / `riemann` / `secant` / `tangent` / `unit circle`)。
引擎必须对所有概念通用。

> 禁止 1 和 3 由 `src/math/architecture.test.ts` **自动检查**。
> ⚠️ 那条正则**刻意不带词边界** —— 因为真实违规长这样:`shellRadius`、`diskGeometry`、`RiemannBars`。
> 早期版本带了 `\b`,驼峰违规全部漏网,是变异测试抓出来的。别"顺手优化"这条正则。

### 引擎通用性的验收方式
每加完一个新概念,跑:

```bash
git diff --stat <上一个提交> -- src/engine/
```

**输出必须为空。** 加一个概念只应该动 `src/concepts/`、`src/math/`、可能加通用的 `src/scene/` 图元。
如果你发现非改 `engine/` 不可 —— **停下来**,说明引擎抽象错了。回去改引擎让它更通用,
**不要在概念里打补丁**,也不要给 `Stage` 加一个只有某个概念用得上的字段。

(已被验证一次:加 Disk Method 整条链,`src/engine/` 零改动。这是可达到的标准,不是理想。)

---

## 2. 数学铁律

1. 任何涉及数学的改动,**必须附手算测试用例**,含边界(n=1、Δx 极大、θ=0、函数有零点/奇点)。
2. **每个量必须有两条独立计算路径**,并互相验证。解析解 ↔ 数值积分,或换一种方法算同一个量。
   单一来源的数字一律不采信,包括你自己算的。
3. 黎曼和一律用**中点法**(`x_i = a + (i+0.5)·Δx`)。理由见下。
4. 数值比较用 `toBeCloseTo(expected, 6)` 或更严。**绝不允许放松容差来让测试变绿。**
5. 屏幕上显示的每一个数字必须来自 `src/math/`,不允许在组件里现算。
6. 写新数学函数时,**先写测试再想清楚数学** —— 反过来会出事。
   (实例:Disk 那条我同时断言了"二阶收敛"和"精确",自相矛盾,测试报 NaN 才发现。)

### 已验证的基准数据(勿改,勿重算)

**Shell / Disk pilot**:`f(x)=4−x²`,`[0,2]`,绕 y 轴 → `V = 8π = 25.132741228718345`
三条独立路径一致:闭式原函数 · 自适应 Simpson · Disk 法对 y 积分 `π∫₀⁴(4−y)dy`

其余概念的钉死数字见 `docs/HANDOFF.md` 各分镜。

### 为什么必须用中点
取中点半径时 `2πr·h·Δx ≡ π(R²−r²)·h` —— **恒等式,不是近似**。
(x₀=1.2, Δx=0.3 → 两边都是 5.790583579,差 8.9e-16)
这是 Shell 公式为什么"精确"的原因,也是产品里最重要的教学点之一。
换成左/右端点会破坏它,连带毁掉 Stage 5b 的全部意义。

---

## 3. 动画与视觉规范

- **相机移动与物体运动绝不同时发生。** 先转相机(物体不动),再动物体(相机不动)。
  这条已经变成**数据约束**:`Stage.autoplay.delayMs` 必须 ≥ 800(相机过渡 `TRANSITION_MS = 1100`),
  `validateChain()` 会拒收更小的值。别绕过它。
- 禁用 `autoRotate`。相机移动必须服务于数学理解,不许自己转着玩。
- 对象**不允许凭空出现或消失**。要有淡入或生长过程,且 narration 要解释它从哪来。
- 配色是**词汇不是装饰**,全站语义一致(见 `src/scene/theme.ts`):
  半径/x 方向 = 红 · 高度/函数值 = 蓝 · 厚度/Δ = 灰 · 当前主角 = 黄 · 本步新引入的量 = 青 · 结果 = 绿
- 公式一次只高亮一行(`validateChain` 强制)。
- 每步 ≤ 1 句 narration + ≤ 4 行公式(`validateChain` 强制上限 4)。
- 60fps 目标,30fps 底线。
  ⚠️ 已知陷阱:让参数连续变化并在每帧重建几十个几何体会直接卡死。
  需要"数量逐渐增加"的效果时用 `Autoplay.steps` 的离散档位(见 Shell stage 7),
  它同时更符合教学意图(要讲"n 翻倍误差缩 1/4",连续变化反而把倍增关系糊掉)。

---

## 4. 工作纪律

1. **一次一个任务。** 不接"把剩下的概念都做了"这种任务,要求拆到单条链、单个 stage。
2. 每个任务完成后交付两样东西:(a) 手算测试用例(若涉及数学) (b) Jesse 该在浏览器里点什么、期望看到什么。
3. 每个任务完成后 `git commit`,并更新 `CLAUDE.md` 第 10 节进度日志(记教训,不只记做了什么)。
4. 提交前必须 `npm run check` 全绿。它同时是**部署门禁** —— 数学不过,站点不上线。
5. **同一个 bug 连续 3 次没修好就停下**,说明你的假设,让人来判断。别继续试。
6. 打磨阶段:**先只列问题清单,不要动手改**,等 Jesse 圈定范围。收尾期禁止大重构。
7. **不确定就停下来问,不要猜。** 停下来问是好行为,会被正向确认。
8. 发现前置条件没做好(依赖没装、文件不存在),先验证事实再动工。

---

## 5. 命令

```bash
npm install
npm run dev      # http://localhost:5173
npm run check    # tsc --noEmit && vitest run   ← 提交前必跑,也是部署门禁
npm test         # 只跑单测
npm run build    # 生产构建
npm run shots    # 构建 + 逐 stage 截图到 tests/e2e/screenshots/(自带静态服务器)
```

### 视觉验证不是可选项
`npm run shots` 需要 Playwright + 能跑 WebGL 的 Chromium。
**单测全绿不代表画面对。** 已经有三个真 bug 是靠截图抓到而单测完全无感的:
相机 target 设错导致曲线顶端被裁在画面外、取样矩形与区域 z-fighting、摊平后的板超出视口。
改动任何场景代码后都要跑一次并**真的看图**。

---

## 6. 部署

- push 到 `main` → GitHub Actions 自动部署到 **GitHub Pages**。这是目前**唯一**能用的部署路径。

### ⚠️ Netlify 现在是坏的,别照着旧说明走

这一节以前写着「主站 Netlify」。**那句话已经不成立了**,而且很误导 ——
照它去验收,会跑去看一个停在几周前的站,然后以为自己刚发的东西没生效。

2026-09-01 查到的事实:

| 项 | 实际情况 |
|---|---|
| 最后一次成功部署 | `2026-08-23`,`deploy_source: "api"`、`commit_ref: null`、标题 `Deploy triggered by upload` |
| 有没有连仓库 | **没有**。是一次性上传,所以 push 再多次它也不会动 |
| 现在能不能部署 | **不能**。新部署直接被跳过:`error_message: "Skipped due to account credit usage exceeded"` |
| 页面上是什么 | 8 张课程卡、没有筛选条、没有 PDF 带子 —— 首页改版之前的样子 |

所以 `calcviz.netlify.app` 现在是一份**过期的公开副本**。要恢复它,需要 Jesse 本人做两件事
(两件都超出代理的权限:一个要花钱,一个要授权 GitHub):
1. 等额度重置,或升级 Netlify 套餐 —— 否则任何部署都会被跳过;
2. 在 Netlify 后台把项目**连上 GitHub 仓库**(需要 GitHub OAuth 授权),
   这样每次 push 自动构建,不会再放着放着就过期。
   `netlify.toml` 已经写好了(`npm run build` → `dist`),连上就能用。

在这两件事做完之前:**以 GitHub Pages 为准**,不要往 Netlify 部署,也不要拿它验收。

Netlify site id:`efa914ff-2725-47d0-99b2-92c1337eda3c`(留着备查)
### 接自定义域名:只需要新建一个文件

⚠️ 这里有一个**只在部署之后才会暴露**的陷阱。GitHub Pages 有两种挂法,base 完全不同:

| 情况 | 站点地址 | base 必须是 |
|---|---|---|
| 没有自定义域名 | `https://jesse090630.github.io/calc-viz/` | `/calc-viz/` |
| **有**自定义域名 | `https://<域名>/` 的**根**上 | `/` |

而部署工作流是**无条件**设 `GITHUB_PAGES=true` 的。所以接上域名的那一刻,
如果 base 还是 `/calc-viz/`,每个资源都会去请求 `https://<域名>/calc-viz/assets/…` ——
**全部 404、整站白屏**,而本地怎么测都是好的(本地永远从根路径起服务)。

所以 base **不再由环境变量单独决定**,而是跟着 `public/CNAME` 走 ——
GitHub Pages 本来就是靠这个文件认自定义域名的,让两者共用同一个事实来源,
它们就不可能再对不上。见 `vite.config.ts` 的 `resolveBase`,
以及 `src/math/deployBase.test.ts`(空 CNAME 与不存在的行为也钉住了)。

**操作步骤(买好域名之后):**

1. 新建 `public/CNAME`,里面**只写域名一行**,例如 `calcviz.app`;
2. 在域名商那边配 DNS:
   - 用二级域名(如 `www.calcviz.app`):加一条 `CNAME` 记录指向 `jesse090630.github.io`
   - 用裸域名(如 `calcviz.app`):加四条 `A` 记录指向
     `185.199.108.153` / `185.199.109.153` / `185.199.110.153` / `185.199.111.153`
3. push。base 会自动变成 `/`,`CNAME` 会被带进 `dist/`,Pages 读到它就切过去。
4. GitHub 仓库 Settings → Pages 里勾上 **Enforce HTTPS**(证书签发要等几分钟)。

⚠️ `deployBase.test.ts` 最后那条断言写着"现在还没有配域名"——
**建了 CNAME 之后它会变红,那是故意的**:提醒你 base 已经切了,DNS 要跟上,
旧的 `github.io/calc-viz/` 链接会开始重定向。改掉那条断言,别绕过它。

- 查 CI 状态(免登录):`https://api.github.com/repos/Jesse090630/calc-viz/actions/runs?per_page=3`
- 部署后**必须实测**:用无头浏览器打开线上地址,进两条链各按几次方向键,确认 canvas 渲染 + console 零错误。
  不要只看到 "Deploy is ready" 就宣布完成。

---

## 7. 现在做什么

打开 **`docs/HANDOFF.md`**。里面有:当前完成度快照、加一个概念的标准流程、
剩余 4 个概念的完整分镜(含已验证的数字)、v2 的设计规格、以及每一项的验收标准。

按队列顺序做,一次一条链。

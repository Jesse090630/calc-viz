# CLAUDE.md — 项目进度日志

> ⚠️ **规则不在这里。** 唯一权威是 [`AGENTS.md`](./AGENTS.md)(三个禁止、数学铁律、动画纪律、
> 部署与验证流程),工作队列与分镜规格在 [`docs/ROADMAP.md`](./docs/ROADMAP.md) 与
> [`docs/HANDOFF.md`](./docs/HANDOFF.md)。
>
> 本文件**只保留进度日志**。原先这里还抄了一份架构与规则,结果不到一天就和 AGENTS.md 漂移了
> (目录结构停留在只有一条链的时候,架构图还写着早已废弃的 scene2d/scene3d 双层)。
> 两处各维护一份规则一定会走偏 —— 所以那部分已经删掉,不要再加回来。

日志的用法:**记教训,不只记做了什么。** 一条"某某功能完成"没有价值,
"某某地方错了、为什么会错、怎么发现的"才有 —— 后来的人靠它避免重犯。

---

## 进度日志

- **2026-08-16 · Phase 0 完成**。Vite 8 + React 19 + TS 5.9 + Tailwind 4 + Vitest 4 骨架建立;
  依赖图在 Linux 沙箱实装验证无 peer 冲突;`npm run build` 通过。
- **2026-08-16 · Phase 1 完成**。`src/math/` 全部纯函数 + 52 个测试通过。
  做了变异测试:植入 4 个数学错误(中点改左端点、解析解漏 2、xF 系数写错、shellSlice 用左端点),
  全部被测试抓到 —— 确认测试有效而不只是"绿"。
- **2026-08-16 · Phase 2 完成**。引擎全部落地,93 个测试通过。
  - `renderScene` 作为 prop 注入 —— engine 不 import 任何场景代码,禁止 3 因此是结构性的而非纪律性的。
  - `Stage.autoplay.delayMs` 把"相机先动、物体后动"变成**声明式**约束,`validateChain` 会拒收 < 800ms 的。
  - Shell Method 的 9 步链数据已写完(Phase 5 的内容提前落地,同时用来验证类型设计够不够用 —— 够)。
  - 场景仍是 `PlaceholderScene`,目的是让分镜和讲解文字先被验收,不必等 3D 写完。
  - ⚠️ 教训:禁止 3 的正则最初写了词边界 `\b`,`shellCount` 这类驼峰违规全部漏网。
    是变异测试抓出来的,不是 code review。**新增架构规则时必须配一个故意违规的变异测试。**
- **2026-08-16 · Phase 3 + 4 + 5 完成**。Shell Method 全链跑通,106 个测试 + 9 张逐步截图,console 零错误。
  - 架构决定:废弃 scene2d/scene3d 双层,改为单一 Three.js 场景 + 相机预设(理由见 §3)。
  - `math/shellSurface.ts`:壳曲面的顶点函数是**纯数学**,不 import three,
    因此"弧长守恒""半径守恒""起点对齐"这些几何性质是**单测**验证的,不是靠看画面。
  - 搭了视觉验证回路:sandbox 里 headless Chromium + swiftshader,`npm run shots` 出 9 张图。
  - **靠截图抓到的三个 bug**(单测全绿时它们都在):
    ① `CameraRig` 没等 OrbitControls 就位就设 target,导致 front 视图一直看着 y=0,
       曲线顶端被切在画面外 —— 单测永远发现不了这种错。
    ② 取样矩形与区域同处 z=0 造成 z-fighting,矩形上有摩尔纹。
    ③ 摊平后的板长 2πx≈7.5,`three-quarter` 装不下会裁掉右端 → 新增 `wide` 预设。
  - 性能:stage 7 原本让 n 连续爬到 96,每帧重建近百个几何体直接卡死。
    改成**离散倍增档位 [4,8,16,32,64]** —— 顺带更符合教学意图(要讲的就是"n 翻倍误差缩 1/4",
    连续变化反而把倍增关系糊掉了)。引擎因此新增了 `Autoplay.steps`。
- **2026-08-16 · 已发布到 GitHub**。https://github.com/Jesse090630/calc-viz(public)。
  用 GitHub Desktop 完成(注意:它的 Local Path 输入框**不展开 `~`**,必须填绝对路径)。
- **2026-08-16 · Phase 7 第一条:Disk Method 完成**。135 个测试,首页 + hash 路由上线。
  - ⭐ **引擎通用性验收通过:`src/engine/` 零改动**(`git diff --stat HEAD -- src/engine/` 为空)。
    第二个概念只写了 chain 数据 + 场景装配 + math 里的 disk 函数。
  - **几何也零新增**:圆盘 = `rIn = 0` 的壳,横着摞而已。`shellSurfacePoint` 原样复用,
    `ShellMesh` 提升为 `scene/RevolutionMesh`(Shell / SolidStack),Washer 以后也用同一个。
  - 教学设计:刻意与 Shell **共用同一个立体**(同区域、同轴),只换切法。
    落点是"两种切法同一个 8π",以及一个几乎没教材讲的反差 ——
    **Shell 的黎曼和有 O(n⁻²) 误差,Disk 的任意 n 都精确**(被积函数 π(4−y) 是线性的,
    中点法对一次函数零误差)。而且圆盘堆出来是阶梯、形状肉眼可见地不对,体积却分毫不差。
  - ⚠️ 教训:我最初给 Disk 同时写了"二阶收敛"和"精确"两个断言,自相矛盾,测试报 NaN 抓了出来。
    **先写测试再想清楚数学,好过反过来。**
- **2026-08-16 · 部署尝试记录**(四条路全试过,结论:都差一次人工点击,别再重复踩)
  | 路径 | 卡在哪 |
  |---|---|
  | Vercel 关联 GitHub 仓库 | 报错 "You need to add a Login Connection to your GitHub account" —— 要浏览器 OAuth |
  | Vercel 直传源码(已成功创建部署) | 站点被 Deployment Protection 拦住,302 跳 SSO;MCP token 无项目写权限,`update_project_deployment_protection` 返回 404 |
  | Netlify MCP | 服务端持续无响应 |
  | GitHub Pages Actions | `actions/configure-pages@v5` 的 `enablement: true` 失败 —— 仓库 Actions 默认只有只读权限,给不了 `pages: write` |

  **一次性解法(推荐 GitHub Pages,因为它带 `npm run check` 门禁、push 即部署、且运行状态可通过公开 API 查)**:
  仓库 Settings → Actions → General → Workflow permissions → 选 "Read and write permissions" → Save。
  之后任意一次 push 即自动上线到 https://jesse090630.github.io/calc-viz/

  查 CI 状态的办法(不需要登录):`https://api.github.com/repos/Jesse090630/calc-viz/actions/runs?per_page=3`
- **2026-08-16 · Phase 7 第二条:Riemann Sum → 定积分完成**。153 个测试,新增 8 张逐步截图,console 零错误。
  - 严格沿“左和高估 / 右和低估 → 同屏夹逼 → 夹缝 = 4Δx → Δx → 0 → ∫”推进,
    `n=4` 显示 `6.250000 / 4.250000 / 5.333333`,`n=64` 夹缝显示 `0.125000`。
  - 新增通用 `scene/RiemannBars.tsx`;矩形位置与高度全部由 `math/riemann.ts` 产出,
    `leftRightGap` 的逐矩形求和路径与端点恒等式路径互证,精确面积另与 adaptive Simpson 互证。
  - ⭐ **引擎通用性再次验收通过:`src/engine/` 零改动**。
  - ⚠️ 教训:首次逐图检查发现矩形填充淡出后轮廓仍残留;透明度必须同时驱动填充与边线,
    不能只凭 `npm run shots` 的退出码判断画面对。
  - ⚠️ 第三条链暴露了跨路由 hook 数量竞态:`ChainPlayer` 直接调用 Scene 函数,Shell / Disk 的 hook 数量
    恰好相同所以一直没暴露;Riemann Scene 无 hook 后切链会报 React #300。无需改 engine,在 `App` 按
    `chain.id` 给播放器加 `key` 让每条链正确重挂载;开发与生产截图序列均验证 console 零错误。
  - 手机竖屏 390×844 实测可用;滑块获焦时方向键只改 n、不翻页,回退再前进参数重置为该步初值。
- **2026-08-16 · Riemann 验收与修正**(接手方复核,不采信自报)。
  - 用项目自己的模块独立复算钉死的数字:6.250000 / 4.250000 / 5.333333 / 夹缝 2.000000,全部吻合;
    并断言**所有 n 下右和 < 真值 < 左和**(这条链的骨架,之前没有测试覆盖)。
  - 补做了交付时漏掉的**变异测试**:左右调换、矩形高度改用中点、解析解加偏差、端点差反号、
    矩形位置改左边缘 —— 5 个全被抓到。
  - 修视觉:`upper Lₙ` / `lower Rₙ` 原本并排飘在图右侧空地上,读者得自己连线。
    改成锚在各自色带上;琥珀那个跟踪**最后一段楔形**(f 递减且凹 ⇒ 它永远最宽),
    并给 y 设下限避免 n 大时压到 x 轴。重复的浮动 gap chip 删掉(公式面板已有)。
  - 首页卡片按**依赖顺序**重排:Riemann 第一(Shell 第 6–7 步建立在它上面),不是按完成顺序。
- **2026-08-16 · Derivative 链完成**。188 个测试,`src/engine/` 依然零改动。
  - 例子 `f(x)=x²`、`a=1` 是为那个恒等式 `m(h) = 2 + h` 挑的:第 4 步把它摊开之后,
    用户能在拖滑块**之前预测**每个数字。极限从"越来越接近"变成"一个看得见的加法项在缩小"。
  - ⚠️ `h = 0` 是 0/0。`secantSlope` **抛错**而不返回 NaN;滑块侧用 `clampH` 推开。
    测试遍历滑块整个行程(含正好落在 0),断言渲染出的公式不含 NaN ——
    NaN 不会让任何东西崩,它只会变成屏幕上一个看不见的错,这是最危险的一类。
  - 新增通用图元:`PointMarker`(支持空心,极限链的"洞"要用)、`RiseRun`(Δx/Δy 直角边)。
- **2026-08-16 · Limits 链完成**。223 个测试,`src/engine/` 第四次零改动。五条链全部上线。
  - 落点是第 5 步:**极限不是函数值**。所以 `g(x)=(x²−1)/(x−1)` 在 x=1 处必须真的算不出来 ——
    `at()` 老实做除法拿到 0/0,返回 **null 而不是 NaN**。NaN 会一路传下去,
    把"这里没有值"悄悄变成一条穿过洞的直线。
  - ⚠️ **与 HANDOFF 4.3 的刻意偏离**:原分镜的跳跃反例 `x/|x|` 在原点附近(y=±1),
    现有 front 相机装不下,而加相机预设要改 `engine/types.ts` 的 `CameraPreset` = 改引擎。
    于是换成**跳跃点仍在 x=1、且左半段与 g 完全相同**的分段函数。
    结果反而更好:两条曲线唯一的差别只有右半段,变量控制干净,对比更锐利,取景框也不用换。
    **这正是"发现要改引擎就停下来重想"这条纪律的价值 —— 逼出来的方案比原方案好。**
  - ⚠️ 测试抓到两个真 bug(都不是容差问题):
    ① `oneSidedLimit` 返回 d=1e-6 处的样本值 `1.999999`,把它当"左极限"显示出去
       正是这条链要破除的混淆。改用**一阶 Richardson 外推**(本链两条曲线在断点附近是线性的,
       所以外推精确),现在显示 2.000000 / 3.500000。
    ② 发散检测判据写错了:检查的是"外推值离样本近不近",结果把 `1/(x−1)`
       判成"收敛到 −110000"。正确判据是**相邻两个样本在互相靠拢**。
  - 变异测试 5 项全抓到,含"偷偷把 g 换成化简后的 x+1"和"画曲线时不在洞处断开"。
- **2026-08-16 · Unit Circle → sin/cos 链完成**。250 个测试 + 7 张逐幕截图,
  `src/engine/` 第五次零改动。
  - 教学落点不是“圆很像波”,而是一个不可破坏的同步关系:圆点和波点都直接读取同一个
    `circlePoint(theta)` 的纵坐标,水平连接线因此逐帧严格对齐;第二圈再叠到第一条轨迹上,
    (2π) 周期就是“回到同一个位置”。
  - 取景没有新增相机预设。用现成 `wide` + 整体平移,并把数学原点明确标成 `O=(0,0)`。
    ⚠️ 第一次截图虽然命令成功、console 也为零,但 (2π) 端点仍被侧栏裁掉;
    把时间轴起点贴到单位圆右切点后,完整 (2π) 宽度才真正进入画面。
  - 变异测试逐函数做:`circlePoint` 故意交换 sin/cos 时 7 项失败;
    `arcLength` 故意丢掉半径因子时 2 项失败。两处恢复后全绿。
  - ⚠️ 浏览器手动验收又抓到一件单测看不见的错:滑块步长原为 0.01,
    数学测试能直接传 (π/6),但真实用户永远拖不到它,所以网页不可能显示精确 `0.500000`。
    改成 (π/12) 档位后,分镜表的所有特殊角都可精确选中,浏览器实测 (π/6) 显示
    `sin θ = 0.500000`。
- **2026-08-16 · T2 六链打磨审计完成**。问题清单在 `docs/T2_AUDIT.md`;
  按 AGENTS.md 的收尾纪律只做审计,没有顺手大重构。
  - 390×844 真实浏览器走完六链 47 幕:无 DOM 横向溢出、console 零错误;
    六链都验证滑块聚焦时方向键只改参数、失焦后翻页、返回时参数重置。
  - ⚠️ 最重要的不是 CSS:自动扫描发现 47 幕中 39 幕 narration 超过一句,
    直接违反 AGENTS.md 的动画文案纪律。未提交的 Unit Circle 7 幕已立即压成单句并加测试;
    其余 32 幕列入问题清单,等待明确打磨范围后再统一改,避免收尾期文案大改。
  - 移动端最明显的视觉问题是 Unit Circle 两端贴边裁切;其余优先项是五条链的底部导航
    需要面板滚动、返回链接遮到绘图区、首页 Limits/Derivative 依赖顺序倒置。
  - 首屏仍静态加载六条链,生产 JS 约 1.435 MB / gzip 约 404 KB;路由级代码分割可行,
    但属于结构性优化,未混进本次审计。
- **2026-08-16 · T3 v2.0 任意函数输入完成**。272 个测试，`src/engine/` 继续零改动。
  - Riemann 页新增 mathjs 表达式与区间输入；`x^2,[0,2]` 会生成一条新的 8 步中点链，
    第 4 步显示 `M₄ = 2.625000` 与独立 adaptive Simpson 值 `2.666667`，没有退化成只报答案的计算器。
  - `CurveSpec.F/xF/sqF` 改为可选；Riemann、Shell、Disk 的闭式入口在字段缺失时自动退回数值积分，
    为 T4 铺好地基而没有提前改 3D 场景。
  - 七类边界都有测试：解析失败、定义域外与奇点断线、负值拒绝、端点发散、`a ≥ b`、
    极端值裁切、防抖；浏览器另实测 `1/x,[0,1]` 禁用构建按钮和 390×844 无横向溢出。
  - ⚠️ 教训：第一次截图命令全绿且 console 为零，但输入面板遮住公式、KaTeX 反斜杠也被补丁转义吃掉；
    只有真的看图才发现。应用后自动收起面板并修正转义后重跑全套截图。
  - 逐函数变异 8 项全部抓红并恢复：表达式求值、奇点断线、负值拒绝、防抖、视口映射，
    以及 Riemann/Shell/Disk 三个数值积分回退。
- **2026-08-16 · T4 v2.1 Shell / Disk 任意输入完成**。279 个测试，`src/engine/` 继续零改动。
  - Shell 接高度函数、Disk 接半径函数；`2-x,[0,2]`、`n=4` 时分别显示
    `8.639380` / `8.246681`，两条 independent adaptive Simpson 路径都给出 `8π/3 = 8.377580`。
  - 没有加相机预设：统一视口映射负责区间和值域，极端值裁切仍保留真实公式数字；Shell 额外
    保留 `x=0`，所以 `[1,3]` 的内孔不会被“为了装进画面”平移掉。
  - Disk 没有假设用户函数可逆，而是新增通用横向曲线/区域图元直接画 `x=r(t)`；这避免了
    把“任意函数输入”暗中缩成“单调且有闭式反函数”。
  - ⚠️ 教训：第一轮浏览器断言钉 `n=4`，页面却从 chain 默认继承 `n=8`；两边数学都对，
    但确定性验收不一致。进入堆叠步骤时显式重置 `n=4` 后，浏览器数字才与清单一一对应。
  - 变异 3 项全部抓红并恢复：Δ 宽度除错、放过负半径区间、把 Shell 的半径零点平移掉。
- **2026-08-16 · T5 v2.2 预设与示例完成**。287 个测试，v2 工作队列完成。
  - Riemann / Shell / Disk 各有 4 / 3 / 3 个手算钉死的预设；共享面板同时提供完整下拉和
    三个“Try this”快捷按钮，选中后立即重验，避免 350ms 键入防抖留下旧的可应用结果。
  - 真实浏览器用三条不同路径验收：Riemann 下拉 Sine arch、Shell 快捷 Cone side、Disk 下拉
    Cone radius，分别继续走到 `2.052344 / 2.000000`、`8.639380 / 8.377580`、
    `8.246681 / 8.377580`，不是只检查输入框被填上。
  - ⚠️ 教训：数学正确的 `Math.PI` 直接插进公式会显示 16 位长小数，仍是失败的教学界面；
    新增纯数学 `formatCoordinate`，让公式与轴标签显示 `π`，并用变异测试保证不会退回长尾。
  - 手机 390×844 强制展开面板验收：预设下拉、快捷按钮、错误提示与操作按钮都可见，
    无横向溢出；全套截图 console 零错误，`src/engine/` 从 T3 到 T5 始终零改动。
- **2026-08-17 · Formula Deck + Trig Derivatives ↔ Integrals 完成**。310 个测试，
  `src/engine/` 第六次零改动。
  - 把 Jesse 的五页 AP Calculus 公式表整理为全站可用的 120 张搜索卡：31 条导数、28 条不定积分、
    15 条定积分，以及定义、求和、面积体积、17 个单位圆标准角、数学记号与全部三角恒等式；
    amber “Why?” 按钮把可解释的卡片连回对应推导，而不是把网站做成输入答案的计算器。
  - 新增唯一一条八幕链：单位圆弦速度 → 切向速度 `(-sin θ, cos θ)` → 两条导数 → 反向得到两条积分；
    `θ=π/3` 屏幕固定显示 `(-0.866025, 0.500000)`，中心差分、adaptive Simpson 与独立 Python
    差分/Simpson 路径互证。
  - 逐函数变异 4 项全部抓红并恢复：弦方向反转、切线改成顺时针、sin/cos 分量互换、分类筛选串栏。
  - ⚠️ 教训一：PDF 文本抽取把第 4 页的单位圆图完全漏掉；“读完 PDF”不能等同于抽出文字，
    必须逐页看图，才会发现要补齐四象限 17 个标准角。
  - ⚠️ 教训二：首轮 `npm run shots` 全绿且 console 为零，第 4 幕 P/Q 标签仍在 `Δθ=0.05` 时重叠；
    逐张看图后把标签锚到点的两侧，并新增 390×844 新链末幕验收，第二轮全套截图才通过。
  - 已发布到 `main`；GitHub Pages Actions 成功，Netlify production deploy `6a83d8bcd52e491f0978a5c6`
    为 `ready`，两站均从 Formula Deck 搜索 Sine 后走到第 8 幕，console 零错误。
- **2026-08-19 · ROADMAP v4 W1 自定义函数输入下线**。310 个测试保持不减，
  `src/engine/` 零改动；Riemann / Shell / Disk 默认只走原有精选曲线，输入框、预设与 “Try this”
  入口由 `src/config.ts` 的 `FEATURES.customFunctionInput` 统一关闭。
  - `src/math/expression.ts`、三条动态链、预设及其全部测试原样保留；临时把 flag 改回 `true` 后，
    三个入口立即恢复，浏览器实际用 `x^2,[0,2]` 重建 Riemann 链并再次读到
    `M₄ = 2.625000` / `∫ = 2.666667`，随后恢复为 `false`。
  - ⚠️ 教训一：只在叶子组件处写“不渲染”虽然视觉正确，静态 import 仍会把 mathjs 塞进首页包。
    把开关分支提到 `App`、仅在开启时加载自定义体验后，首包从 gzip 601.17 kB 降到 414.04 kB；
    这不是 W6 的全站路由拆分，七条内置链仍保持原加载方式。
  - ⚠️ 教训二：截图脚本原本无条件寻找输入面板，功能正确下线后反而超时。现在脚本同时守住两态：
    flag 开时继续跑原来的动态输入数字验收，关时明确断言三条链都没有入口；最终全套截图 console 零错误。
- **2026-08-19 · ROADMAP v4 W2 快速修复包完成**。310 个测试保持全绿，
  `src/engine/` 零改动；首页依赖顺序改为 Limits → Derivative → Riemann → Shell → Disk →
  Unit Circle → Trig Rates，全站交互统一使用 3px amber `focus-visible` 焦点环。
  - 390×844 下 Unit Circle 第 7 幕改用现有 `wide` 相机配合更矮的画布构图，单位圆与完整
    `0 → 2π` 时间轴同时入镜；没有新增 `CameraPreset`。长课导航固定在视口底部，并给滚动面板
    留出等高内边距，公式、滑块不会被导航盖住。
  - Formula Deck 分类条保留原有横向滚动，右缘新增渐隐与 amber 箭头；手机端实测分类内容
    确实溢出可滚，页面本身没有横向溢出。
  - 浏览器验收不只看样式声明：首页 8 个、Unit Circle 手机末幕 11 个实际 Tab 停靠点逐个读取
    computed style，全部得到 3px 可见焦点环；完整 `npm run shots` console 零错误。
  - ⚠️ 教训一：播放器导航的语义标签是 `nav`；第一次把测试/样式定位写成 `div`，截图流程在
    Unit Circle 手机断言处超时。改为语义选择器后，又单独读取导航边界为 `top=777, bottom=844`
    才重跑全套，说明验收脚本本身也必须被验证，不能把定位器失败误判成产品失败。
  - ⚠️ 教训二：逐个 Tab 会自动把首页滚到最后一个卡片；若直接截图，数学内容全对但首屏证据是错的。
    焦点审计后显式回到页面顶部，再生成首页验收图。
- **2026-08-19 · ROADMAP v4 W3 首页重构完成**。310 个测试保持全绿，`src/engine/`
  零改动；首页第一屏不再是文字目录，而是直接复用 Riemann 第 5 幕的可拖「夹逼实验台」。
  - Hero 固定同一条曲线、同一个场景和唯一一个 `n` 滑块，不自动播放；`n=4` 显示
    `4.250000 / 5.333333 / 6.250000`，拖到 `n=8` 显示
    `4.812500 / 5.333333 / 5.812500`，学生能直接看到两侧估计向中间收紧。
  - 七张卡片直接使用已验收的逐幕截图作为静态缩略图，并按 Foundations / Integration /
    3D Volume / Trigonometry 四条依赖轨道分组；图片懒加载，没有给七条链各开一个自动播放场景。
  - 七条链的最后一步都有确定的 `Recommended next →`。入口在 UI 路由包装层读取既有 store，
    不给 `Stage` 加概念字段、不改播放器引擎；末幕浏览器断言逐链核对目标 hash。
  - 390×844 首页首屏能同时看到场景、命题、三项数字、滑块与完整入口，无横向溢出；首页
    10 个 Tab 停靠点、Unit Circle 末幕 12 个停靠点全部保留 3px 焦点环，全套截图 console 零错误。
  - ⚠️ 教训一：第一版把推荐入口浮在场景底部，功能断言会绿，但它压住了 Riemann 的
    `A = 5.333333` 标签。逐图发现后把推荐入口嵌到末幕导航中央，只替换进度点，保留左右箭头，
    不再覆盖场景、公式或 Unit Circle 的滑块。
  - ⚠️ 教训二：自动化先 `fill()` hero 滑块会留下焦点；随后按“可见元素数量”循环 Tab，最后一次
    会跳出页面并被误报为缺焦点环。焦点审计前重新载入页面、从首个真实停靠点开始，验收脚本
    才和用户实际从页面顶部键盘导航的路径一致。
- **2026-08-20 · ROADMAP v4 W4 Calc Type Board 完成**。317 个测试全绿，`src/engine/`
  零改动；全站工具条现在把 114 张 Formula Deck 公式卡与 31 张 Type Board 符号卡并排但分工明确。
  - 独立 `#/notation` 路由按 Operators / Change & limits / Relations & logic / Functions & structure /
    Greek alphabet 分组；每张卡都钉死 symbol、name、say、means、example、confusion 六个字段，
    五张代表卡另有“读法与误解必须具体”的回归断言。
  - 搜索同时覆盖符号、正式名称、读法和外观描述；真实浏览器验证 `dee ex` 找到 `dx`，
    `stretched S` 找到 integral，Relations 恰好 8 张，Integral 的 Why 跳到 Riemann。
  - ⚠️ 教训一：新板子写完后复审发现 Formula Deck 还留着 6 张旧 notation 卡；“两个入口都能查”
    不等于职责清楚。删掉重复展示并加边界测试后，Deck 才真正只装公式、Board 才只装符号。
  - ⚠️ 教训二：第一轮自动验收把 CSS 转成的大写状态文字当成失败；修正断言后逐图又发现手机滚动时
    sticky 搜索栏会贴住全站工具条。把它锚到工具条下方后重跑完整 7 链截图，console 零错误。
  - 桌面与 390×844 手机截图都人工看过；31 张卡、读法搜索、分类、Why 跳转、焦点环与无横向溢出
    均由浏览器路径覆盖。本工单没有新增数学量，因此不适用双路径计算或数学变异测试。
- **2026-08-20 · ROADMAP v4 W5 无障碍补齐完成**。323 个测试全绿；55 个内置分镜都新增了
  一句独立的 WebGL 画面等价描述，当前步号与公式通过 `aria-live="polite"` 播报。
  `src/engine/` 只改了本工单唯一预先批准的 `Stage.altText` 类型，没有加入概念字段或额外引擎行为。
  - Formula Deck 与 Calc Type Board 现在都是真正 portaled 到应用根节点之外的 modal dialog：打开后
    搜索框自动获得焦点，应用背景 `inert`，Tab / Shift+Tab 在弹窗内循环，Esc 或关闭按钮退出后
    焦点回到各自入口。Type Board 仍保留可分享的 `#/notation` 地址。
  - `prefers-reduced-motion: reduce` 命中时，由引擎外的 chain adapter 隐去 autoplay 声明并把参数
    直接初始化到终值；相机也直接就位。真实 Chromium reduce 模式进入 Unit Circle 第 2 步后，
    50ms 内即显示 `θ = 1.047198`，没有 1200ms 延迟或补间。
  - ⚠️ 教训一：最直接的实现会改 `engine/useAutoplay.ts`、`ChainPlayer.tsx` 与 `FormulaPanel.tsx`，
    虽然逻辑通用，仍违反“W5 只有 Stage.altText 获批”的边界。把 autoplay 适配、隐藏播报区和
    scene 描述都提升到 App 无障碍层后，同样完成需求，同时把引擎差异压回唯一批准的 2 行。
  - ⚠️ 教训二：弹窗 Escape 的自动验收最初只等 20ms 就读取 React 状态，真实交互正确却被误报；
    改为等待 dialog 实际消失后再检查 `inert` 与焦点归还，Formula Deck 和 Type Board 两条路径
    才都成为稳定的回归测试。完整桌面/390×844 截图已人工查看，console 零错误。
- **待办**:ROADMAP v4 W5 已完成；下一项是 W6，按 AGENTS.md 一次一个任务，等待 Jesse 确认。
- **2026-08-20 · W6 路由级懒加载完成**。首页 JS 从 gzip **391 kB → 63.5 kB**,336 测试,
  55 张分镜与基线逐张比对最大差异 1.22%(软件渲染的抗锯齿噪声),`src/engine/` 自 W5 起零改动。
  - ⚠️ **最大的教训:`React.lazy` 本身不省流量。** 组件一旦被渲染,它的 chunk 立刻就下载。
    第一版把七条链、Home hero、两块参考板全改成 lazy,首页仍然是 391 kB ——
    因为 hero 和 FormulaDeck **在首页就被渲染了**。
    真正省下来的是这两刀:
    ① hero 默认显示 Riemann 第 5 步的**真实截图**,点一下才把 3D 换进来(省 241 kB);
    ② FormulaDeck 拆成"按钮留主 chunk + 弹窗 lazy",且**只在 open 时才挂载**(省 75 kB)。
    判断有没有做对不能靠读代码,要用真浏览器数实际下载量。
  - `src/concepts/registry.ts`:首页元数据的唯一来源,**刻意不 import 任何 chain**。
    以前 `CARDS` 里写 `LIMITS_CHAIN.stages.length`,看着只是取个数字,
    实际把七条链和 Three.js 一起拖进了首页。代价是 `steps` 成了手写常量 ——
    由 `registry.test.ts` 对着真链兜底。**它当场就抓到我把 trig-rates 的标题抄错了。**
  - `FormulaDeck` 与 `NotationBoard` 现在形态一致(受控 open/onClose/returnFocusRef),
    触发按钮都在 App 里。之前两块板子一个自带按钮、一个不带,是不必要的不对称。
  - 截图脚本跟着改:hero 现在要验**两个状态** —— 先断言封面在且 canvas 数为 0
    (证明 3D 没有偷跑),点亮后再断言 canvas 起来。
  - ⚠️ 环境坑:整套 shots 在这个 3.9 GB 沙箱里跑到第 4 条链会静默退出,
    单独跑每条链则 14 秒全过、零错误。**不是代码回归**,是残留 chromium 进程累积。
    诊断时先隔离复现,再改代码 —— 我差点去改一个根本没坏的东西。
  - ⚠️ 又踩了一次 `pkill -f` 匹配到自己命令行的坑(exit 137)。模式要用 `[c]hrome-linux` 这种写法。
- **待办**:W7(∫dx/x 为什么是 ln)。完整队列见 `docs/ROADMAP.md`。
- **2026-08-20 · W7「为什么 ∫dx/x 是 ln」完成**。392 测试(+69),`src/engine/` 自 W6 起零改动。
  - 教学落点不是"背下这个特例",而是**幂法则在 n = −1 处是坏掉的**:分母为 0。
    第 2 步把 n 拖向 −1,定积分一路答到 0.693171,然后区域变红、读数变成 `undefined (0/0)`。
    这和 limits 链第 1 步是同一个可去间断。
  - 第 5 步是支点:`[1,2]` 横拉 2 倍、纵压 1/2 倍后**正好盖住 `[2,4]`**,两块都是 `0.693147`。
    `1/x` 是唯一在这个变换下回到自己的幂函数 —— 这才是对数出现的真正原因。
  - ⚠️ **主路径一次都没调用 `Math.log`**。面积全走 `adaptiveSimpson`,
    e 用二分法从 `A(t)=1` **找**出来而不是读 `Math.E`。
    用标准库的对数去算这块面积,等于把要推的结论当前提。
    `architecture.test.ts` 新增一条规则盯着它 —— 并用变异测试确认过那条规则真的会红。
  - ⚠️ **显示坐标必须是纯缩放,不许平移**。`X = 0.85x`,`Y = 3.2/x`。
    「横拉 b」是关于原点的伸缩;一旦加平移,`x → bx` 在显示坐标里就不再是伸缩,
    第 5 步的支点当场塌掉。测试里专门断言 `toX(x·b) === toX(x)·b`。
  - 变异测试 7 项全抓到,含"n=−1 时偷偷返回极限值""给分母加个小量绕过 0/0"
    "主路径改用 Math.log"。
  - ⚠️ **线上抓到的 bug**:把新链加进 `registry` 却没配首页缩略图,
    `ConceptLink` 直接抛错、**首页一张卡都渲染不出来**。
    单链截图完全看不到 —— 我那一轮只跑了 log-integral 一条链。
    只有真的打开部署后的首页才撞上。已加测试守住:每个概念必须有缩略图。
    **教训:加链之后要看的不只是新链,还有首页。**
  - 📌 **发现一个既有问题(不属于 W7,未修)**:右上角 Type board / Formula deck 工具条
    压住了课程面板的 "DERIVATION CHAIN · … · STEP n OF m" 标题,**八条链全中**。
    是 W4/W5 引入的,按"一次一个 W、不混提交"的纪律没有顺手改。
- **2026-08-22 · 首页清空成空白板**(Jesse 要腾地方放新东西)。392 测试不变,`src/engine/` 依旧零改动。
  - ⚠️ **是封存,不是删除。** hero + 四条轨道 + 八张卡 + THUMBNAILS 整体搬到
    `src/ui/ConceptGrid.tsx`,一行没改、没人 import。八条链**全部还在线**,
    `#/limits`、`#/log-integral` 直接输网址照常能开,只是首页不再列出来。
    恢复目录 = 在 `Home.tsx` 里 `import { ConceptGrid }` 渲染一次。
  - 因为没人 import,Rollup 整个 tree-shake 掉了,连九张 `new URL(...)` 的图都没进产物 ——
    **封存不占体积**。首屏 273 → 263 kB。
  - 缩略图守卫**故意留着**并改指向 `ConceptGrid.tsx`。目录回归的那一刻缺图会让首页整个白掉
    (W7 就是这么炸的),而那时候没人会想起来。宁可现在就红。
    两个变异都确认能抓:少一张图、以及把 `export const THUMBNAILS` 改名让守卫自己失效。
  - `shots.mjs` 的首页一节改成验**清空得干不干净**:0 卡片 / 0 canvas / 两块板还在且键盘可达,
    外加一条"封存的链路由还能直达"。空白板还偷偷拉 Three.js 就等于白清。
  - 📌 右上角工具条压住课程面板 "STEP n OF m" 标题那个老问题**仍未修**(八条链全中)。
    首页现在是空白板所以看不见,但一进课程页还在。
- **2026-08-22 · 新增 `src/labs/` 与第一节实验台「Every Pair Must Work」**(严格递增的形式定义)。
  524 测试(+109),`src/engine/` 依旧零改动。
  - ⭐ **这是项目里第一个"不是推导链"的东西。** 它要教的是"任取一对",
    根本不存在"第几步",硬套上一步/下一步会把 ∀ 讲成一串例子。
    所以新开 `src/labs/`,**不经过 `src/engine/`**,段落靠"做过什么"解锁而不是翻页。
    "故意不用引擎"和"忘了用引擎"在代码里长得一样,所以钉成了两条架构测试:
    labs 不许 import `/engine/`,也不许 import `three`(这一节全程二维,
    拉进 3D 引擎等于白费 W6 那一轮的功夫)。两条都用变异测试确认过会红。
  - ⚠️ **命名即立场**:判定函数一律叫 `...OnGrid`,状态叫 `holds-on-grid`,
    **绝不叫 `proved` / `isIncreasing`**,并有测试断言状态名里不许出现 prov(ed|en)。
    有限抽样不能证明 ∀ 命题;代码里先说错了,界面上一定跟着说错。
    反过来 `refuted` 可以叫证明 —— 一个反例是真的够。这个不对称就是整节的主题。
  - 两条独立判定路径:全对枚举 C(n,2)(定义本身) vs 相邻比较 O(n)(靠传递性)。
    在 6 个函数 × 5 个关系 × 4 个区间上断言结论一致。
  - 加了一块需求里没有的**「所有对」地图**:横轴 x₁ 纵轴 x₂,合法的对铺满对角线上方的三角形。
    扫描往里打点。理由是"扫过很多对"和"全部"在直觉上仍是两回事,扫得越顺越像"已经试完了"。
  - ⚠️ **文案被自己的截图打脸,改了。** 原话是"打了 1,200 个点,三角形仍然几乎是空的"。
    截图一看:满满当当。文字和画面对着干,学生只信眼睛,反而得出"那就是全试过了"。
    改成先承认"看起来是满的",再讲为什么满不等于全。**写完要看图,不能只看测试绿。**
  - ⚠️ **浏览器抓到的真 bug(单测全绿时它在)**:把手柄拖到区间尽头,
    两点相等 → `orderPair` 返回 null → 上层回退 → **两个点当场弹到区间两头**。
    用户看到的是"我在往右拖,点却瞬移到最左边",而这一节从头到尾讲的就是保持 x₁ < x₂。
    修法不是给回退分支打补丁,是让相等**根本不可能发生**(永远留一条最小缝隙),
    并把这段逻辑抽到 `pairState.ts` 以便单测遍历整个行程。
  - ⚠️ **"最有说服力"的反例不等于最好懂的那个**。按"输出落差最大"挑,
    x² 在 [-2,2] 上给出 `(-2, -0.051)` → `4` 与 `0.0026`:第二个点几乎压在**顶点**上
    (而顶点是后面"分成两段"那一幕的主角,提前用掉就剧透了),而且贴着 x 轴读不出来。
    改成"找掉头往下的那一段,取左端点与中点",得到 `(-2, -1)` → `4` 与 `1`。
    另外把结果 snap 到整数 —— 但**先验证再采用**,为了好看动出一个不是反例的对,
    等于把教学事故写进代码。
  - 变异测试:monotonicity 7 项、pairState 4 项,含"严格递增偷偷放成 ≤""缝隙设为 0"
    "snap 不做验证就采用"。**两项最初没被抓到**(平坦段的浮点噪声、分界点不跳过平坦段),
    因为测试只用 x² 试过,而 x² 根本没有平坦段 —— 补了三条针对分段函数的测试才堵上。
  - 无障碍:拖拽 / 原生滑块 / 方向键三条通路;成立与否同时给符号(✓ ×)、文字和颜色,
    颜色只是第三重信道。
  - 📌 右上角工具条压住课程面板标题那个老问题**仍未修**(推导链八条全中,本节不受影响)。
- **2026-08-22 · 第二节实验台「The Symmetry Test」(奇偶函数)+ 抽出 `src/labs/shared/`**。
  577 测试(+53),`src/engine/` 依旧零改动。
  - **先抽公共件再写第二节**:`viewport` / `Tex` / `DraggableXPoint` / 配色状态词汇搬进 `labs/shared/`。
    ⚠️ 顺带把写死的 `VIEWPORT` 常量改成 `makeViewport()` 工厂 + prop ——
    对称性这一节需要**上下对称**的窗口(y ∈ ±9.5),常量写法当场卡住:
    要么传参,要么复制一份图元。复制迟早两边漂。搬完重新在浏览器里验了上一节没坏。
  - 两条独立判定路径:**数值取样**(比较 f(-x) 与 ±f(x)) vs **指数奇偶**
    (非零项全偶 ⇒ 偶,全奇 ⇒ 奇)。推理方式毫无重叠,测试断言两者在每个多项式上一致,
    并另外核对"系数求值 == at()",否则两条路径其实在算不同的函数。
  - ⚠️ **提示词给的五个函数里一个 neither 都没有**(x²、x³、|x|、x、x²+1)。
    三选一里永远有一个答案不会对,学生两三题就学会不选它 —— 学到的是猜题不是定义。
    补了 x²+x 与 x³+1,并有测试盯着"三种答案都出现过"。
  - ⚠️ **反例不能取"第一个撞上的"**。x³+1 的第一个偶性反例在 x ≈ 0.0075:
    `f(-0.0075)=0.9999996` vs `f(0.0075)=1.0000004`,数学上没错,
    屏幕上两点**完全重合**,拿它当"看,不相等"只会让人觉得被骗。
    改成取**差距最大**的那个。这和上一节"反例落在抛物线顶点上"是同一类错。
  - ⚠️ **浏览器实测抓到:反例够不着。** 判定默认 radius=3,给出的反例是 x=3.00,
    而滑块最远只到 2.2 —— 界面上白纸黑字写着"把 x 拖过去看看",学生拖到头也到不了。
    **给一个够不着的证据比不给还糟。** 现在把滑块行程传进判定函数,并有测试守着。
  - ⚠️ **两处标签重叠,都是截图看出来的**:
    ① "same height" 放在两点正中间 —— 但两点关于 y 轴对称,中点**永远是 x=0**,
       必然压在 y 轴刻度上。挪到线段 28% 处。
    ② 原点同时写了 "rotation centre" 和 "midpoint",两行字糊在一起。合并成一句。
  - 措辞纪律延续上一节:拖一个 x 看见成立**不给结论**,要看过 3 个不同的 x 才放出
    EVEN/ODD 卡片,而且说的是"每一个都对得上"。反例那边则可以直接下结论 —— 一个就够。
  - 变异测试 9 项,含"奇性判据写成 f(-x)=f(x)""指数奇偶判反""题库删掉 neither"。
    ⚠️ 有两项最初没抓到:容差放宽、以及把 x=0 算进取样点 ——
    前者是我对 x³+1 的失效方式判断错了(差距恒为 2,与 x 无关),
    后者是"取差距最大"之后 x=0 自然竞争不过别的样本,那行 `i = 1` 其实已经不是防线。
    **把注释改成说实话,而不是继续宣称它在防什么。**
- **2026-08-22 · 第三节实验台「Does It Repeat?」(周期函数)**。622 测试(+45),`src/engine/` 依旧零改动。
  - 教学落点**不是**"2π 是周期",而是**「看起来重合」不算数**。
    所以界面上始终挂着一个读数:`worst mismatch = max |f(x+T) − f(x)|`。
    T = 6.2 时它是 `0.083157` —— 两条曲线肉眼完全分不出来,那个数就是不肯变成 0。
    判断因此从"我觉得对上了"变成"读数是多少"。
  - 两条独立路径求基本周期:**数值搜索**(从最小档位往上试) vs **公式 2π/k**。
    另断言"基本周期之下没有更小的周期",以及"每个整数倍都真的是周期"。
  - ⚠️ **滑块必须真的能停在 2π 上。** CLAUDE.md 里 unit-circle 那一节踩过同一个坑
    (步长 0.01,测试能传 π/6,用户永远拖不到)。这里 T 吸附到 **π/12** 的整数倍,
    π/2、π、2π、4π 全部精确可选。
  - ⚠️ **但吸附把最有价值的教学时刻也吸没了。** 一吸附,6.2 立刻变成 2π,
    学生**永远碰不到**那个"看起来完全重合、其实差 0.08"的值 ——
    而那正是 `worst mismatch` 存在的全部理由。
    补了一个明写着的 `T = 6.2 …?` 按钮绕过吸附。
    **自己加的约束把自己的教学点挡住了,这种事只有把整条路走完才会发现。**
  - ⚠️ 截图抓到两个:① x 轴 3π / 4π 显示成 `9.424778` —— `math/format` 的
    `formatCoordinate` 只认到 ±2π,整条轴的 π 语言当场破功;改用本模块的 `formatShift`。
    ② "off by 2.00" 放在红色竖线的中点,而那条线跨越 ±1,中点正好落在 x 轴上,
    读数压住了 "−2π" 刻度。改放到较高那个点的上方。
  - ⚠️ 预设按钮里是 KaTeX,读屏拿到的是一堆 MathML 碎片,读不出"派 over 二"。
    是测试先撞上的(`getByRole('button', {name: 'π'})` 找不到任何东西)——
    **测试找不到,读屏用户同样找不到。** 补了 `aria-label` 与 `aria-pressed`。
  - `T = 0` 单独处理:偏离恒为 0 但判定为假,并解释定义里那句 `T > 0` 就是为了排除它。
  - 变异测试 8 项,含"容差放宽到 0.1""去掉 T>0""吸附步长改 0.01""公式漏掉频率"。
    ⚠️ 一项没抓到:把取样窗口砍到不足一个周期,测试**全绿** ——
    说明当时没有任何东西守着窗口宽度。补了 `SAMPLE_SPAN` 与一条显式断言。
- **2026-08-22 · 第四节实验台「Connect Two Points」(平均变化率 / 割线斜率)**。
  660 测试(+38),`src/engine/` 依旧零改动。顺带把 `pairState` 也提到 `labs/shared/` ——
  第二个需要"两个 x 不许重合"的地方出现了,再复制一份就一定会漂。
  - ⭐ **第二条独立路径是个恒等式,不是"换个写法再算一遍"**:
    对 x²,`(b²−a²)/(b−a) = (b−a)(b+a)/(b−a) = a + b`。
    平均变化率**恰好等于两个输入之和**,a=1、b=3 → 4,不用算就知道。
    这条路径一次除法都不做,所以能真正独立验证差商;
    它同时是这一节最好的教学点 —— 学生可以在拖动**之前预测读数**。
    等他试过 3 组之后才揭示,免得剥夺自己发现的机会。
  - ⚠️ **Δx = 0 是这一节唯一的雷**。`0/0` 在 JS 里是 NaN,而 NaN 不会让任何东西崩,
    它只会变成屏幕上一个看不见的错。`averageRate` 在 a===b 时返回 **null**,
    界面显示 `undefined (0 / 0)`;拖动侧则由 `pairState` 保证两点永不重合,双保险。
  - 直角三角形的直角顶点放在 **(b, f(a))**,不是 (a, f(b))：
    水平边贴 A 的高度、竖直边贴 B 的横坐标,「先走 Δx 再爬 Δy」的顺序
    与 `(f(b)−f(a))/(b−a)` 的读法一致。
  - ⚠️ **页脚那句结论把悬念提前泄了**。原本常驻着
    "平均变化率和割线斜率是同一个数",于是学生还没拖任何东西就读到了答案,
    整个"两点 → Δx → Δy → 相除"的搭建过程当场作废。
    改成只在最后一步之后出现。**是测试先撞上的**(断言"公式没有一上来就摆出来"失败)。
  - ⚠️ 截图抓到:B 落在右端时,`B = (3.00, 9.00)` 与 `Δy = 8.00` 都被视口裁掉半截。
    标签改成按位置决定写在哪一侧,并加了一条"所有 svg 文本都在画布内"的浏览器检查。
  - 「把两点靠近」刻意在**还看得见两个点**的地方停下,并且**全程不出现 derivative 一词**
    (测试断言了这条)。只留提示词允许的那一句"In calculus, we will ask…"。
  - 变异测试 7 项,含"Δx=0 返回 NaN""恒等式写成 a−b""rise 写成 f(a)−f(b)"。
    ⚠️ 一项没抓到:去掉"靠太近就停"的守卫 —— 因为 gap*=0.82 在有限步内根本到不了 0,
    "不许重合"那条从来没被考验过。那条 break 真正在防的是**两点靠到肉眼分不开**,
    所以补的断言是"最后一步仍然分得开",不是"不等于零"。
- **2026-08-22 · 首页改成四张带循环动画预览的卡片**。660 测试不变。
  - 每张卡顶上一小段 SVG,循环播该节课最核心的那个动作(两点滑动 / 镜像点同进同出 /
    副本右移一个周期 / 割线绕 A 转)。静态图说不清"这是能动手的",动一秒就说清了。
  - ⚠️ **不 import 任何实验台组件**,预览是几十行独立 SVG;曲线来自 `src/math/` 的纯函数
    (禁止 2 不破)。首屏 263 → **276 kB**,只多了 13 kB,仍然零 canvas。
    W6 那一轮的成果没有被这次改版吃掉。
  - ⚠️ **四张卡共用一个 rAF 时钟**(`ui/previews/clock.ts`)。
    各开一个循环是最顺手的写法,那意味着首页常驻四个循环、每帧四次 setState。
  - ⚠️ `prefers-reduced-motion` 时**根本不启动 rAF**,停在一个有代表性的静止相位,
    并在页脚明说"预览已暂停" —— 免得有人以为坏了。不是"动得慢一点",是不动。
  - ⚠️ 视口比例踩了一次:第一版 viewBox 240×116(比 2.07),而卡片实际约 530×116(比 4.6),
    `preserveAspectRatio="meet"` 按高度缩放,左右各留一大块空,画面缩在中间一条,
    看着像没做完。加宽到 400 才填满。
  - ⚠️ 周期那张卡:副本落位后两条曲线完全重合,等宽的话绿色把蓝色整个盖住,
    卡片看起来只有**一条**曲线,"副本盖住原图"这件事反而消失了。底下那条加粗到 3.2,
    蓝色就成了绿线的描边。
  - ⚠️ **验证"预览在动"这件事本身错了三次**:
    ① 取每张卡的第一个 `<path>` —— 那是**静止的曲线**,四张里三张被误判成静止;
    ② 只隔 500ms 取两帧 —— 周期那张在两端有**刻意的停顿**,很容易正好落在停顿里;
    ③ 拿 `innerHTML.slice(0, 300)` 当指纹 —— 会动的元素排在静止曲线后面,根本没进这 300 字。
    正确做法:取**最后一个**几何元素的关键属性,跨越一整个循环采样。`shots.mjs` 已按此改写。
- **2026-08-22 · 第五节实验台「Drop to the Integer」(取整函数 ⌊x⌋)**。
  696 测试(+36),`src/engine/` 依旧零改动。首页同步加到五张卡(含第五个循环预览)。
  - ⭐ **主路径不调用 `Math.floor`**,而是把定义写出来:先朝零截断,
    再检查 —— 截断结果如果**大于** x,说明越过头了,往下退一格。
    那个"退一格"的分支就是负数修正本身,**在代码里看得见**。
    `Math.floor` 留给测试当第二条独立验证路径(与 logIntegral 不用 `Math.log` 同一条纪律)。
  - ⚠️ 这一节全部的重量都在**负数**上:⌊-1.3⌋ = **-2**,不是 -1。
    测试里把常见错法逐个列出来并证明它们错(`Math.trunc` / `~~` / `Math.round` / `parseInt`
    在 -1.3 上全给 -1),同时证明它们在**正数上碰巧都对** ——
    所以只测正数会漏掉这一节的全部内容。
  - 界面上负数不是"提一句",是**同时三处**:图上把 n+1 圈红并标注 `-1 > -1.3`、
    定义两条逐条列出(一条 ✓ 一条 ×)、外加那段红色警告。只写文字学生不会读。
  - ⚠️ **浮点在整数处会骗人**。连续累加 0.1 得到 0.9999999999999999,
    ⌊x⌋ 于是给 0,而屏幕上写着 x = 1.0 —— 矛盾正好落在最关键的位置。
    滑块值一律吸附到精确十分位。
  - ⚠️ **这条测试第一版是假的**:我用"累加 30 次 0.1"当例子,而那个值是
    3.0000000000000013,落在 3 的**上方**,取整照样给 3 ——
    压根没碰到要防的坑,去掉吸附测试依然全绿。真正会出事的是累加 10 次和 50 次。
    **教训:挑例子时要先确认这个例子真的会失败,否则测的是空气。**
    现在逐个整数都试,并另加一条断言"这批值里确实有落在整数下方的"。
  - ⚠️ 截图抓到两个:① 数轴挤在上半部分,轴线以下一大条空白 —— 纵向范围按内容收紧;
    ② **同一个 x 画了两个琥珀点** —— 我自己画了一个,`DraggableXPoint` 又在 y=0 画了一个,
    中间还连着一段莫名其妙的虚线。改成 y=0 就是 x 那一行,点只由手柄画。
  - 阶梯图 y 轴刻度紧贴原点、压在台阶线上,用深色描边(`paintOrder="stroke"`)让文字穿过线,
    比挪位置干净 —— 挪走就离轴太远,对不上是哪一行。
  - 浏览器扫过滑块**全部 81 个位置**,逐个与 `Math.floor` 比对,全部一致且无 NaN。
  - 变异测试 6 项:删掉向下修正、修正方向反、比较写成 >=、去掉吸附、闭/开端点搞反等。
    (`Math.round` 替换 `Math.trunc` 那一项没红 —— 查下来它其实是等价写法,不是缺陷,已在注释里说明。)
- **2026-08-22 · 第六节「Jump to the Integer」(上取整)—— 但真正做的是把第五节**泛化**。
  707 测试(+11),首页六张卡。
  - ⭐ **一个组件,两节课。** `floorFunction.ts` → `rounding.ts`(两个方向同一份),
    `FloorViews` → `RoundingViews`(方向作为参数),`FloorLab` → `RoundingLab`(默认方向作为 prop)。
    `#/floor` 与 `#/ceiling` 只差标题和起始值。
    复制一份 CeilingViews 是最省事的写法,也是最糟的 —— 改个箭头样式要改两处,迟早只改一处。
  - ⭐ **两个方向的陷阱位置正好相反,这是这一对课最值钱的地方**:
    下取整被"朝零截断"骗在**负数**(⌊-1.3⌋ = -2);
    上取整被骗在**正数**(⌈4.2⌉ = 5)。而截断在各自的另一侧**碰巧是对的** ——
    所以只试一侧会得出"截断就是取整"的错误结论。警告文案跟着方向走,不是写死的。
  - 第三条独立路径:**⌈x⌉ = −⌊−x⌋**。不碰截断逻辑,靠"把数轴翻过来"。
    连同 `Math.ceil` 一共三条路互证。
  - ⚠️ **阶梯图的开闭端点两个方向相反**:⌊⌋ 是 [n, n+1) 左实心,⌈⌉ 是 (n-1, n] 右实心。
    画反了图形看起来一模一样,但整数处取值全错。所以 `closedOnLeft` 是数据的一部分,
    不是组件里写死的"左实心右空心",并有测试逐格断言。
  - ⚠️ **截图抓到一个真矛盾**:比较模式下数轴按"当前方向/另一方向"配色,
    右侧面板按"下取整/上取整"配色 —— 从 `#/ceiling` 进来时,
    ⌈⌉ 在数轴上是绿的、在面板里是青的,**同一屏两半自相矛盾**。
    改成颜色一律绑定**方向**(下取整绿、上取整青),并验证两个入口进来颜色一致。
    这正是 `RoundingViews` 开头那句"各自算一遍迟早自相矛盾"的翻版 —— 只不过这次矛盾的是颜色。
  - ⚠️ `Math.ceil(-0.9)` 返回 **-0**,而 `toBe` 走 `Object.is`,`Object.is(+0,-0)` 是 false。
    我们刻意把 -0 规范成 +0(否则屏幕上显示 "-0")。测试改用 `===` 比数值,
    另加一条**专门断言永远不返回 -0**,而不是假装两者完全相同。
  - 变异测试 7 项全抓到:删掉向上进位、比较写成 <=、反射恒等式漏负号、
    阶梯高度写成左端点、closedOnLeft 两边都 true、rejected 方向反、去掉 -0 规范化。
  - 浏览器两个方向各扫 81 个滑块位置,与 `Math.floor` / `Math.ceil` 逐个比对,全部一致。
- **2026-08-22 · 第七节实验台「One Input. One Output.」(函数的定义)**。
  753 测试(+47),首页七张卡,`src/engine/` 依旧零改动。
  - ⭐ 全节压在一个**不对称**上,而学生和实现者都容易记反:
    一个输入 → 两个输出 ✗ / 两个输入 → 一个输出 ✓。
    所以测试**每一条都成对**断言:证明前者被拒、且证明后者被接受。
    只测前者的话,一个"看到任何重复就拒绝"的错误实现照样全绿 ——
    而那个实现恰好会把最重要的教学案例(共享输出)判反。
  - 两条独立判定路径:**按输入分组比较** vs **数去重后的个数**
    (不同输入数 === 不同对数 ⟺ 是函数)。后者不分组也不记忆,推理完全不重叠。
    另在 300 个定种子随机关系上断言两者一致。
  - 界面上"共享输出"那个输出用**青色虚线圈**(不是警示色),并主动写一句
    "Different inputs may share the same output" —— 学生看到重复的 5
    第一反应就是喊"不是函数",界面必须抢在那句话之前把它标成合法。
  - 垂线测试的两条路径:**枚举分支** vs **数实根**。
    ⚠️ x = y² 在 **x = 0 处两支重合,是 1 个交点不是 2 个** ——
    那个点正是"从 0 个变成 2 个"的分界,报成 2 会让"什么时候开始有两个"讲不清。
  - ⚠️ 变异测试有一项没抓到:把 `offendingInput` 改成"只要输入重复就返回它",
    原本测试全绿 —— 因为我从没拿"同一输入配**相同**输出"的关系问过它。
    那个实现会在一个完全合法的关系上把箭头标红,是界面上看得见的错。已补测试。
  - ⚠️ **浏览器测试误报了两次**:我靠搜文字判断当前结论,
    而页面底部那张对照表里**永远**写着 "× not a function"。
    结论是状态,不该靠读文案反推 —— 改成给判决卡挂 `data-verdict` 属性。
    (同一类错在这个项目里出现第三次了:KaTeX 读不出、CSS uppercase、现在是常驻文案。
     **凡是要断言"当前状态",就给它一个属性,别搜文字。**)
- **2026-08-22 · 第八节实验台「Where Is x Allowed?」(定义域)**。
  798 测试(+45),首页八张卡,`src/engine/` 依旧零改动。
  - ⚠️ **这一节最危险的不是数学,是 JS 的两个"假值"**:
    `Math.sqrt(-2)` → **NaN**,`1/0` → **Infinity**。两个都不是实数,**但都不报错**。
    放任它们流出去,屏幕上会出现 "f(-2) = NaN" 或一个飞到无穷远的点 ——
    看起来像"有值",而这一节要教的恰恰是"这里没有值"。`at()` 一律返回 **null**。
  - ⭐ 两条独立路径:**声明的区间**(纯符号,带开闭端点) vs **真的算一遍**(纯数值)。
    另外显式在边界点上再测一遍 —— 十分位网格未必踩得到别的边界,
    而边界正是唯一会错的地方,不能指望网格碰巧覆盖。
  - ⭐⭐ **端点方向相反,这是学生真正会错的地方**:
    √x 在 0 处**有**值 → 闭,实心;1/x 在 0 处**没**值 → 开,空心。
    所以实心/空心由数据(`closedFrom`)决定,不是组件里写死的。
  - ⚠️ **真 bug:1/x 的曲线会被连成一条穿过渐近线的假线**。
    等距取样网格未必踩得到 x = 0 —— [-3,3] 取 601 个点时 0 落在第 300.5 个位置,
    一个 null 都不会出现,两侧于是被连起来,正好把这一节要教的洞抹掉。
    靠"点跑太远会被裁掉"来断线是碰运气。现在 `sampleCurve` **显式把洞塞进取样点**,
    并测了 5 种取样点数(奇偶都有)。
  - ⚠️ 截图抓到三个:① "ALLOWED INPUTS" 压在 x 轴刻度数字上;
    ② 准入条挂在 `y0 + 30`(相对 x 轴),y=0 的画面高度随取景变化,
    条子飘在中间、底部预留的空白白占一大片 —— 改成锚在画布底部那条预留带里;
    ③ **两段允许区间被发光晕成一条连续的绿条**,而面板写着 `(-∞,0) ∪ (0,∞)`,
    "两段"在图上完全看不见 —— 开端点各留 3px 缝隙(洞是单点,缝画宽了是另一种谎)。
  - ⚠️ `DOMAIN_RANGE` 是 `as const`,直接拿它当默认参数会把类型窄化成字面量 `-4`/`6`,
    传别的数字编译不过。默认参数要显式标 `number`。
  - 变异测试 9 项全抓到,含"不挡负数让 NaN 流出去""√x 端点写成开""1/x 端点写成闭"
    "取样时把 null 换成 0""去掉显式塞洞"。
  - 浏览器三个函数各扫 101 个滑块位置,与各自的条件逐个比对,并断言全程不泄漏 NaN/Infinity。
- **2026-08-22 · 首页改版:金句标题换成概念名,去掉大标题、角标与页脚说明**。
  798 测试不变(纯版式与文案),`src/math/` 与 `src/engine/` 零改动。
  - ⭐ **目录的标题要用概念本身的名字**(Domain of a Function / Even and Odd Functions),
    不是「Where Is x Allowed?」这类金句。金句是**课内**的钩子,读一遍有劲;
    但目录是拿来**找东西**的 —— 找的人心里想的词是"定义域",不是那句话。
    课页顶上的 h1 一并改掉:目录写一个名字、点进去顶上是另一个名字,是最招人烦的不一致。
    (原来的金句没扔,都降级成了课页的副标题。)
  - 顺手把八张卡按**教学顺序**重排:函数定义 → 定义域 → 递增 → 奇偶 → 周期 → 平均变化率 → 下取整 → 上取整。
    原来的顺序是"我按写完的先后堆上去的",对读的人没有意义。
  - ⚠️ 预览盒子从写死的 `h-[7.25rem]` 改成 **`aspect-[400/116]`**。
    SVG 是 `meet`:盒子比例与 viewBox 一旦对不上,图就缩在盒子中间、上下露黑边,
    看着像没加载完 —— 而列数随断点变(手机一列/平板两列/桌面三列),写死高度必然在某个宽度上翻车。
    浏览器里直接量 `svg.getBoundingClientRect()` 与父盒子的差,超过 1.5px 就算 letterbox。
  - ⚠️ 大标题一删,第一排卡片会往上顶,而右上角工具条是 `fixed` 的 —— 加了一条**矩形相交**断言,
    专门盯"卡片钻到工具条底下"。这种事截图里未必看得出(工具条有背景色,压上去像是设计),
    但鼠标点不到那一块。
  - 新增 `tests/e2e/home-check.mjs`:只跑首页,不跑八条链(几秒 vs 几分钟)。
    断言"页面上看不到"而不是"代码里没写" —— 删文案这种活,漏删一处就是白改。
- **2026-08-23 · 第九节实验台「Nondecreasing Functions」(非递减,`x₁ < x₂ ⟹ f(x₁) ≤ f(x₂)`)**。
  846 测试(+48),首页九张卡,`src/engine/` 依旧零改动。**未部署**(Jesse 的 Netlify 额度快到了)。
  - ⭐ 这一节和第三节(严格递增)只差一个符号,所以界面**不重讲**"任取一对"那一套,
    全部重量压在符号上:两点落在平坦段时,`<` 与 `≤` 在同一屏上给出**不同**的答案。
    `<` / `≤` 的对照表**常驻**,不是只在平坦时才弹出来 —— 只在平坦时出现的话,
    学生记住的是"平坦时有个提示",而不是"两条规则一直都在,只在平坦处分道扬镳"。
  - ⭐ 图用**连续分段线性**而不是公式:哪一段平、哪一段升必须**写在数据里**,
    不能靠采样去猜。用 sin/多项式的话"这里是平的"只能是近似,
    而这一节要教的恰恰是"输出**相等**"这件确定的事。
    两张图刻意**首尾相同**(都从 (0,1) 到 (8,5)),差别只在中间那一次下降。
  - ⭐⭐ **「两端输出相等」≠「中间这一段是平的」**。
    非递减图上两者等价(往上走一步就再也回不来);但**有下坡**的图上先上后下也能回到同一高度 ——
    dip 图上 f(3.5) = f(5.5) = 2.5,中间既有上坡也有下坡。
    所以 `spanIsFlat` **单独按段的斜率算**,绝不从 `y1 === y2` 反推;
    反推的版本会指着一段起伏说"这里是平的",是一句看得见的假话。
  - ⭐ 两条独立路径:求值 = 段内插值 vs 从左端累加斜率×长度;
    一对的关系 = 比两端输出 vs 累加净涨跌;
    整图判定 = 格点逐对检查 vs **逐段斜率 ≥ 0**。
    最后这一对还有教学价值:逐对检查再多也证明不了 ∀,而"每一段都不下降"**是**证明。
    界面上那句结论用的是后者,措辞里没有"我们查了很多对"。
  - ⚠️ 变异测试 20 项全抓到。其中 `snapX` 用 `floor` 替 `round` 一开始**活下来了** ——
    我挑的测试值(2、4、3.5)除以 0.1 都除得尽,两者答案相同。
    但拖动传进来的是任意实数,floor 会让每次拖动系统性偏左半格、右端点永远够不到。已补测试。
  - ⚠️ 吸附是最容易把 `x₁ < x₂` 压成相等的一步(1.96 与 2.01 各自吸到 2.0)。
    `snapPair` 保证吸附之后仍然差至少一格,并且顺序传反了也能纠回来。
  - ⭐⭐ **新增一条通用检查:图上任意两行 SVG 文字不许叠在一起**(按包围盒相交面积算,
    超过较小者的两成就算)。这一节靠它抓到三处我肉眼没看出来的重叠:
    ① 等高时两个坐标标签 + "same height" 叠成 `s(0:4he1i0)t`;
    ② 两个输出读数「一上一下」的规则写反了 —— 该按**谁高谁低**决定上下,
       写死"x₁ 在上、x₂ 在下"时,y₁ < y₂ 的情形下两枚牌子反而相向而行;
    ③ 下坡段的 "↘ down" 与当前这一对的 "↘ went down" 撞在一起。
    标签位置随两点移动,肉眼抽查必然漏。**这条检查值得搬去别的实验台。**
  - ⚠️ 浏览器扫描的第一版**扫了个寂寞**:x₁ 起点取 0 / 2.5 / 5,dip 图上永远从低处往高处看,
    'down' 一次都没出现过,全绿 —— 而那正是这一节最要紧的状态。
    现在会**记录扫描过程中真的见到过哪几种形状**,少一种就报错。
  - ⚠️ 期望值在检查脚本里**按图形重写了一遍**分段公式,不从被测模块 import ——
    否则测的只是"渲染层有没有把数字原样搬过去"。
  - `DraggableXPoint` 加了可选的 `labelDx`:两个手柄挨到最近时两个居中标签会叠住。
    默认 0,其他课不受影响。
- **2026-08-23 · 第十节实验台「Nonincreasing Functions」(非递增,`x₁ < x₂ ⟹ f(x₁) ≥ f(x₂)`)**
  —— 但真正做的是把第九节**泛化**成一个模块两个方向。862 测试(+16),首页十张卡。**未部署**。
  - ⭐ 和第五 → 第六节(下取整 → 上取整)同一套做法:**不复制,改成参数**。
    `nondecreasing.ts` → `weakMonotonicity.ts`,`labs/nondecreasing/` → `labs/weakMonotone/`,
    两个路由(`#/nondecreasing`、`#/nonincreasing`)指向同一个 `WeakMonotoneLab`,只差一个 `direction`。
    复制一份改符号的话,两边的容差、格点、端点会各自漂移,
    最后同一个概念在两页上给出不同答案 —— 而这两页恰恰只差一个符号。
  - ⭐⭐ **允不允许是「方向」的属性,不是「走向」的属性。**
    原来 `SHAPE_COPY.up` 上挂着 `allowed: true` —— 在非递减那节是对的,
    照搬到非递增就会指着一段上坡说"允许"。已经把 `allowed` 从走向上删掉,
    一律问 `isAllowed(direction, shape)`。组件里一处方向判断都没有:
    哪个箭头被禁、哪个符号、三格按什么顺序排,全从 `DIRECTION[direction]` 读。
  - ⭐ 新图:非递增用**降 → 平 → 降**(斜率 −1, 0, −1),不合格的那张是**降 → 升 → 平 → 降**。
    同一节课的两张图**首尾完全相同**(非递减 1→5,非递增 5→1),差别只在中间反了一次向。
  - ⭐ 「输出相等 ≠ 中间是平的」这条陷阱在**两张**不合格的图上都测到:
    dip 图 f(3.5) = f(5.5) = 2.5(先上后下),bump 图 f(2) = f(5) = 3(先下后上)。
  - ⭐ 新增一条跨模块的一致性测试:`DIRECTION[d].weak/strict` 指向的关系
    必须在 `monotonicity.ts` 的 `RELATIONS` 里满足 `allowsFlat` 为真/假。
    两个模块各写一份"平坦算不算数",迟早对不上。
  - 变异测试 12 项全抓到,包括「isAllowed 写死成非递减」「非递增的 forbidden 写反」
    「readPair 写死方向」「非递增配错了严格版本」「verdictBySlopes 写死找下坡」。
  - ⚠️ 换课时组件被复用,状态会停在上一节的图上 —— 于是非递增那页会画出非递减的折线。
    加了一个 `useEffect([direction])` 复位,并在浏览器检查里**正面对比两页**:
    曲线的 `d`、`data-relation` 列表、被禁的那一格,三样都必须不同。
    "共用一个组件"最容易出的错就是两页长得一模一样,这条专门盯它。
  - 浏览器检查同样泛化成两节课跑同一套断言,416 个滑块位置。
    起点里必须包含犯规段的左端(dip 是 4.0,bump 是 3.0),否则 forbidden 一次都不会出现 ——
    上一轮正是这么空跑过。扫描过程中见到过哪几种走向仍然逐图记录并断言。
- **2026-08-23 · 第十一节实验台「Increasing and Decreasing Intervals / Scan the Curve」**
  (在一段区间上函数是增、减,还是不变)。914 测试(+52),首页十一张卡。**未部署**。
  - ⭐⭐ **这一节的全部价值在于"跨过转折点时拒绝给出单一答案"。**
    学生最常犯的错是比一下两端就下结论 —— 而在跨过极值的区间上,
    两端比较**一定**会给出一个确定但错误的答案。
    有一条测试专门**搜出**这种窗口(两个方向各一个)并断言两条判定路径都说 mixed。
    ⚠️ 那条测试第一版把窗口写死成 (2, 4) 并断言两端比较说"升" ——
    实际那一段两端比较说的是"降"。坑是真的,方向我猜错了。改成搜,顺便证明两种都存在。
  - ⭐ 曲线由**转折点生成**,不是先写多项式再去找拐点:
    屏幕上写「递增于 (−∞, 3)」必须逐字为真。相邻转折点之间用余弦缓动(两端斜率为零、
    严格单调、拼接处 C¹),最外两条臂用抛物线 —— 于是那两个 ∞ **是真的无界**,
    不是把有限端点写成无穷。有测试走出去 100 个单位确认它还在往那个方向跑。
  - ⭐ 两条独立路径:**查窗口压到了哪几段**(纯符号) vs **窗口内密集取样逐对比较**(纯数值)。
    ⚠️ 逐对路径第一版写成"有上升的、没有下降的 → 递增" —— 于是"先升后平"被判成递增。
    可 f(3.6) = f(3.9),那是**非递减**,不是递增。两条路径当场打架,洞才露出来。
    「递增」要求**每一对**都严格上升。
  - ⚠️ 变异测试 22 项,有四项**活下来**,四项都是真的测试洞:
    ① `monotoneIntervals` 里"合并相邻同向段"的分支**永远走不到**(按构造相邻段走向必不同)——
       删掉死代码,改成把那条不变量直接测出来;
    ② `sampleCurve` 显式塞入拐点那段删掉也全绿 —— 因为默认取样数下拐点恰好都是格点。
       改成用除不尽的取样数(7 / 13 / 33)再测一遍。定义域那一节吃过同一个亏;
    ③ 平坦判定的容差从 EPS 放大到 0.5 全绿 —— 三条曲线的落差要么是 0 要么大于 3,
       中间那一档没有数据。造了一条落差 0.2 的 fixture 曲线专门顶住,
       并把 `valueAt` 里的平台判定改成复用 `behaviourBetween`,只留一处"什么算平"的定义;
    ④ "拖左端把手却把整条带子往右推"照样全绿 —— 原断言只查了宽度和顺序。
       补上"另一端必须钉住"。
  - ⚠️ 走向的配色**不沿用别处的对/错语义**:递减不是错误,它只是另一个方向。
    ↗ 绿 / ↘ 琥珀 / → 青,红色只留给 **mixed** —— 那才是"现在给不出答案"的状态。
    小圆点用**它此刻所在那一段**的颜色,于是走过转折点时当场从绿翻成琥珀。
  - ⚠️ 挑战的回答有**三种**不是两种:提示词只列了"全程递增"与"中途变向",
    但学生完全可能选中一段全程**递减**的区间 —— 含糊地说"behaviour changes"会把他教糊涂。
  - ⚠️ 浏览器检查里那段"通过"的 fixture 第一版落在 mixed 上(先拖左端会被最小宽度顶住,
    后面的按键全部落空),整个通过分支根本没被测到。是"fixture 必须真的是递增的"那条断言抓出来的。
    216 个扫描位置,期望值在检查脚本里按拐点表**另写一遍**求值,与页面不共享代码。
- **2026-08-24 · 第十二节实验台「One-Sided Limits / Two Sides. One Destination.」**。
  953 测试(+39),首页十二张卡。这是 Jesse 一次给的七节微积分课里的第 1 节。
  - ⭐⭐ **这一节的代码里,单侧极限一次都不读 `f(a)`。** 不是"碰巧没用到",是硬约束:
    下一节课整节在讲"点决定不了极限",这一节的实现里只要有一条路径用了端点函数值,
    界面就已经在暗示它有关。有一条测试**把 `valueAtA` 换成 null / −99 / 0 / 3.5 / 1e6**,
    断言两个单侧极限和双侧结论一个都不许动。
  - ⭐⭐ **两侧不一致时 `twoSidedLimit` 返回 `null`,绝不返回数字。**
    左 2 右 5 时给出 3.5(折中)、2 或 5(偏袒一侧)都是屏幕上看得见的假话。
    界面上那一行由 `twoSidedTex` 出,DNE 时写的是 "does not exist",没有等号跟数字。
  - ⭐ 两条独立路径:**那一支的闭形式在 a 处取值**(精确 —— 每支都是多项式,连续)
    vs **沿 10⁻¹…10⁻ᵏ 走一串取样过去**(纯数值)。
    ⚠️ 数值那条叫 `approachedValue` 不叫 `limitOf`:有限次取样说明不了极限存在。
  - ⚠️ **左点永远越不过 a**,这是交互层的不变量(`clampToSide`),不是一句提示语。
    变异测试把上界从 `a − MIN_GAP` 放宽到 `a` 就被抓住。
  - ⚠️⚠️ **`DraggableXPoint` 的 `aria-valuenow` 精度原来写死成两位** ——
    这一节步长 0.001,于是读屏把 1.999 播报成 "2.00":用户听到的是"我到 2 了",
    而这一节整节都在讲**到不了**。改成跟着 `step` 算位数。
    (是浏览器检查先撞上的:它从 aria 读 x,每一条都报"和 f(x) 对不上"。
     后来把检查改成读**屏幕上显示的**那个 x —— 学生看到的是这一对,它们必须自洽。)
  - ⚠️ 变异测试 14 项抓到 12 项,2 项是**等价变异**(`stepCloser` 的 fallback:
    `MIN_GAP` 恰好等于序列最小档,两种写法输出完全相同)。顺手把那段死分支改成显式的"停住"。
  - ⚠️ 另外三处只有截图/浏览器检查能发现:
    ① 提示词的 Part 1/2/3 做成了 **focus 开关**而不是步骤 —— 三个模式随时可切、
       两侧面板始终在 DOM 里(检查会数面板个数,少一个就算做成了 slideshow);
    ② `2 ≠ 5` 那行字原来朝右写,直接顶出取景框右缘;
    ③ 两个点的坐标标签贴到画面边缘时会擦边出界 —— `getBBox` 那条 0.5px 容差的检查
       抓不住"正好压在边上",所以改成**贴边就翻向内侧**。
  - ⚠️ 结论也挂成 `data-limit` / `data-limit-tex` 属性:面板里的等式是 KaTeX 渲染的,
    `innerText` 读出来什么形状不受控。这个项目里因为读渲染后的文案误判过三次,不再犯。
- **2026-08-24 · 一次做完 Jesse 给的七节微积分课**(第 12–18 节)。
  1084 测试,首页十八张卡。七节分别是:One-Sided Limits / Limit vs Function Value /
  The Epsilon-Delta Definition / Infinite Limits / The Squeeze Theorem /
  Why sin x / x → 1 / From Secant to Tangent。
  - ⭐⭐ **贯穿这七节的一条线:极限是"附近的行为",不是"那一点的值"。**
    代码层面把它做成了**签名约束**而不是纪律:
    `limitAtHole()`、`squeezedLimit()`、`specialLimit.squeezedLimit()` 都**不接参数**,
    `oneSidedLimit` 不读 `valueAtA`。"极限依赖函数值"这句话在类型上就写不出来。
    测试直接断言 `fn.length === 0`,并把 f(a) 换成 null / −99 / 3.5 / 1e6 逐个验。
  - ⭐⭐ **JS 的三个静默假值在这七节里全出现了一遍**,每一个都返回 `null`:
    `0/0 = NaN`(限值 vs 函数值、割线在 h = 0)、`1/0 = Infinity`(无穷极限)、
    `sin(0)/0 = NaN`(sin x / x)。每处都先用一条测试**证明这个坑存在**,再断言我们没踩。
  - ⭐ **"不许把 ∞ 当数"** 在无穷极限那节做成了可执行的约束:模块里没有 `limitAt` 这种出口,
    只有 `beats(side, bound)` —— 你给界,我给出超过它的那一档。措辞检查禁止 equals/reaches infinity。
  - ⭐ **弧度不是叮嘱,是事实**:sin x / x 那节把角度制的答案 π/180 ≈ 0.01745 摆在页面上。
    看见那个不同的数,比听十遍"记得用弧度"管用。
  - ⚠️ **同一类死代码出现了四次**:`x === 0` / `|x| <= EPS` 的显式判断在
    `Number.isFinite(1/x)` 之后永远走不到。四处全部删掉 —— 留着会让人以为保证来自那里。
    (pairState 那次立的规矩,这一轮反复验证。)
  - ⚠️ **"注入一个会违规的函数"成了固定招式**。ε–δ 与夹逼两节里,
    "整段扫描"退化成"只看端点 / 取样很稀"都**对现有数据完全等价**,变异测试全绿。
    解法是给检查函数加一个**仅供测试注入**的 `fn`,再造一个只在窄区间里越界的 fixture。
    这类"契约只被现有数据检验"的洞,靠读代码看不出来。
  - ⚠️ **显示精度错了三次,每次都是屏幕上的假话**:
    ① ε–δ:`toFixed(2)` 把 δ = 0.005 显示成 0.01,"0.01 ≤ 0.01"看着刚好卡住,实际差一倍;
    ② sin x / x:四位小数下 θ = 0.02 时 `sin θ / θ / tan θ` 全显示成 0.0200,
       把这一节唯一要证的**严格**不等式写成了三个相等的数;
    ③ 单侧极限:`DraggableXPoint` 的 `aria-valuenow` 写死两位,读屏把 1.999 播报成 "2.00" ——
       告诉用户他到了那个这一节说**到不了**的地方。三处都由浏览器检查按**显示值**判定抓到。
  - ⚠️ **看不见的东西要么放大、要么明说**:ε = 0.01 时横带只有半像素高;
    1/x 第 3 档以后曲线贴到轴上。两处都改成跟着参数缩放,并在角上写出放大倍数 ——
    悄悄放大是另一种骗人。
  - ⚠️ **文字碰撞扫描继续还债**:夹逼那节三个读数在收拢后必然重叠 ——
    改成合成一行"三个已经分不开了",那本来就是结论,不是渲染故障。
  - ⚠️ 首页预览两个新坑:`Number('1e-2.35')` 是 **NaN**(动画用连续档位),
    以及"最后一个几何元素"是静止的目标点导致整张卡被判成不动 —— 把静止的挪到前面。
- **2026-08-25 · SPECIAL LIMITS 一整节完成**(七个新路由,25 张目录卡,1265 个测试)。
  链条按提示词要求排:**代入失败 → 不定式 → 比较增长率 → 发现特殊行为 → 解释为什么 → 复用**。
  - 新增 `indeterminate`(0/0)、`tan-over-x`、`cos-over-x`、`cos-over-x2`、`exp-over-x`、
    `log-over-x`、`special-limits`(变形练习 + 参考卡);`sin-over-x` 补上前两幕。
  - ⭐ **五节课共用一个 `RatioLab`**,只差一个 `FormId`(和非递减/非递增同一条路子)。
    每课自己那一块(割线 / 镜像 / 共轭 / 半角 / 三数并排)从 `extras.tsx` 插进去 ——
    共用骨架讲不了只属于一课的东西,硬塞会把骨架撑坏。
  - ⭐⭐ **这一节的支点写成了结构**:`substitutionForm` 对四个例子(以及六条极限)
    返回**完全同一个字符串**,而 `answerOf` 给出四个互不相同的结局。
    「代入丢掉了区分它们的信息」因此是一条**可被机器验证**的事实,不是一句口号。
  - ⭐ 「由哪条推出」也是结构性的:`factors` 与 `groundedIn` 必须**恰好有一个**为空,
    依赖图无环、每条都能走回 sin x/x 与 (eˣ−1)/x 这两个根。六条里只有两条是"根"。
  - ⭐ 因子分解被证明是**逐点恒等式**:`factorProduct(id, x) === ratio(id, x)` 对每个 x 成立。
    共轭、半角、换元这些代数步骤因此是被**验证**过的,不是被声称过的。

  ⚠️⚠️ **这一轮最重的一条教训:浮点会在屏幕上写出假数,而且不报错。**
  - `1 - Math.cos(1e-8)` 在 double 里**精确地等于 0** → `(1−cos x)/x²` 显示成 `0.0000000`,
    而它的极限是 ½。解决办法不是回避,是用**这一课自己要教的那条恒等式**去算:
    `1 − cos x = 2 sin²(x/2)`。同理 `expm1` / `log1p`。
    教科书写法保留成 `naiveNumerator`,专门在界面上**把这件事摆出来**——
    阶梯深到 10⁻⁸ 时那一列整个塌成 0,红色标注,旁边就是正确的 0.5。
  - 而阶梯**必须够深**才看得见:到 10⁻⁶ 为止两列都写 0.4999…,差别在第七位以后。
    摆一列看不出差别的数字等于什么都没演示(浏览器检查量出「最大差 4.45×10⁻⁵」时发现的)。
  - 三处**显示层的假数**,全是一张截图抓到的,不是测试:
    各处手写 `.toExponential(1).replace('e+','×10')` 会把 **1 写成 "1.0×100"**、
    **10⁵ 写成 "1×105"**、**10⁻⁴ 写成 "1×10⁻4"**(负号是上标数字不是)。
    统一成 `format.ts` 的 `showScientific`(指数是真上标),顺带修好了旧的无穷极限那一课。
    ⚠️ 改完之后 squeeze 与 infinite 两个检查**当场炸了** —— 它们在用 `Number()` 吃显示值。
    炸了是好事:`Number('1×10⁻⁴')` 是 NaN,而 **NaN 参与的比较恒假**,
    那两个检查本来会静静地变成永远通过。新增 `tests/e2e/parse-shown.mjs` 作为 `showScientific` 的逆。

  ⚠️ **变异测试抓到两个"两条路其实是一条"的洞**(21 个变异全部被抓,但先补了这两处):
  - `substitutionForm` 写死成 `'0/0'`,整套测试**全绿** —— 因为这四个例子的结果本来就都是 0/0,
    写死和算出来在这份数据上完全等价。抽出 `substitutionFormOf(top, bottom)`,用非零输入钉住。
  - `limitByFactors` 直接 `return form.limit` 同样全绿。抽出 `productOfLimits`,用合成因子表钉住。
  - `multiply` 里的约分去掉也全绿 —— 因为 `rationalTex` 自己又约了一次,两处各约一次等于没人负责。
    改成直接断言 `answerByPattern('cos-4x-3x2')` 返回 `{num: 8, den: 3}`。
  这三条是同一个形状:**当"整段成立"在现有数据上退化成"端点成立"时,得造一个数据把契约钉住**
  (ε–δ 与夹逼那两节的可注入函数是同一招)。

  ⚠️ 其它靠**读截图**而不是靠测试发现的:
  - 单位圆上 θ 拖到 0.02 时,sin θ / θ / tan θ 三段全缩到 (1,0) 附近,三个标签叠成一团。
    旧检查每次比对前都 reload,θ 回到 1,所以一直没看见 —— **只在滑块另一端出现的 bug,
    而滑块的另一端正是这一课要人去的地方**。标签改成钉在轴上下方与角平分线上,
    并给旧检查补了一个跨 θ 的重叠扫描。
  - 「How Fast Does Cosine Flatten?」后面又接了一个句号 → `?.`。
  - 同一栏里 ③ 排在 ① 上面、三栏读出来是 ③⑤④ —— 编号和阅读顺序打架,两处都重排了。
  - `(4x)^2` 直接塞进散文里,屏幕上写着 `(4x)^2`。同一个字符串既当 TeX 又当散文用,总有一头错。
    新增 `plainTex`,并断言所有 `note` / `fix` 里不出现 `^`。
  - 竞速条按比例画的话,x = 0.01 时两根条差**两个像素**。开三次方压回可视范围,
    并在界面上写明「长度是压缩过的,旁边的数字是精确的」(和无穷极限那节"放大了就说出来"同一条规矩)。

  ⚠️ 两条**检查自身**的教训:
  - `locator.click().catch(() => {})` 把一次 30 秒的超时伪装成一次无害的失败;
    六节课乘五步就是十几分钟,看起来像卡死。改成先 `count()` 再点。
  - 变异测试脚本跑到一半被杀,`specialForms.ts` **带着变异留在盘上**。
    脚本开头改成无条件从 `.bak` 恢复 —— 变异测试自己不能变成污染源。
- **2026-08-26 · 代数一节完成**(四个新路由,29 张目录卡,1413 个测试)。
  平方差 / 立方差 / 二项式定理 / 等比级数。四课共用 `src/labs/algebra/shared.tsx`。
  - ⭐⭐ **剪拼与切块是"验"出来的,不是"画"出来的。**
    `tilesExactly` / `partitionsExactly` 在目标区域里外撒点,断言里面每点被**恰好一块**
    盖住、洞里一块也盖不到、外面没有外溢。只验"面积之和相等"是**不够**的:
    两块互相压住一半、外面再露一块,面积照样能对上,而那种图什么都没证明。
  - ⚠️ 撒点判定必须用**半开区间**。闭区间在公共边上会把一个点算给两块 ——
    (a,b) = (9,7)、宽 16 撒 24 点时第 14 个点正好落在接缝 x = 9 上,
    于是一个**完全正确**的剪拼被判成拼不上。立方差那边同一个坑,同一个解法。
  - ⭐ 三条独立路径算同一个量(定义 / 几何 / 因式),整数运算一律 `toBe` 不用
    `toBeCloseTo` —— 差一点点就是差。
  - ⭐⭐ 二项式那课的落点是「系数 = 方案数」:界面上那六行小格子就是
    `selections(4,2)` 直接画出来的,**图和公式是同一件事**,不是两件恰好一致的事。
  - ⚠️ 阶乘写法算组合数**早在 n = 23 就给出错的整数**(253.00000000000003),
    因为中间量 23! 已经冲出安全整数范围;n = 171 时更直接算成 **Infinity**,
    而真值只有 14535。所以对外的 `choose` 走**递推**(只做加法,一路精确),
    阶乘那条只当第二验证路径。我原本随手拿 (40,20) 去演示这个差别 ——
    那一组误差恰好抵消,两条路给出同一个数,**演示失败的例子本身是错的**;
    扫一遍才找到真正的第一个分歧点。

  ⚠️⚠️ **平方差那一课后来被整个重做成分五幕的证明动画**(Jesse 的第二轮要求):
  Build → Remove → Cut → Rearrange → Factor,每一幕**绑一个数学操作**
  (面积=乘法、拿走=减法、切开=分解、挪动=重排、拼接=加法),写在画面旁边。
  - ⭐⭐ **等号是结论,不是布景。** `(a−b)(a+b)` 要到第五幕过半才出现,
    `a² − b² = (a−b)(a+b)` 要到最后才出现。这条由 `areaTexAt` / `identityTexAt`
    保证,浏览器检查在**每一幕**上断言"结论面板不存在"。
  - ⚠️ **整数进度的歧义**:`g = 1` 是「第一幕走完」还是「第二幕刚开始」?
    时间轴上是同一个瞬间,必须挑一个。第一版用 `Math.floor` 挑了后者,
    于是点一下 Next 从第一幕跳到 g=1 却被读成第二幕 —— 五幕只走得到四幕,
    而且每一幕都少看了它的结尾。改成 `ceil(g) - 1`:整数读作「已走完几幕」。
  - ⚠️ 抵消的标记要**留着**,消失的是宽度。第一版写成 `collapsing && …`,
    两项塌掉之后标记反而没了,检查读到 `no,no,no,no`。

  ⚠️ 靠**读截图**而不是靠测试发现的(这一轮又是四条):
  - `4 \times 10 = 40` 原样显示在页面上 —— 那个字符串被当小字直接渲染,不走 KaTeX。
    **同一个字符串既当 TeX 又当散文用,总有一头是错的。** 改名 `…Plain`,
    并加断言:当散文用的字符串里不许有反斜杠。立方差那边同样一处。
  - `a − b = 4` 的括号横着画在图形**上方** —— 数字对,位置在暗示它量的是上边(a=7)。
    「剩下的厚度」是竖直方向的量,括号就得竖着画在那段厚度旁边。
  - 标签溢出:边距按**格**留(0.7 格)、标签偏移按**像素**写(+20)。
    一格有多少像素取决于 a,于是 a=7 排得下、a=12 溢出。
    **两个单位系统混着用**是这类溢出的根源;统一成像素之后就与 a 无关了。
  - 等比级数的 r 滑块上界定在 1.6,而提示词点名的例子是 **r = 2**(3+6+12+24+48=93)——
    那组例子在页面上根本做不出来。**范围不是凭手感定的,是被要讲的例子定的。**

  ⚠️ 首页动画检测器本身有三处漏网,是这一轮补上的:
  ① 选择器里没有 `polygon` —— 整张图由多边形组成的预览取到 `undefined`,
     签名恒为空串,"没动"和"取不到"长得一模一样;
  ② 只读 `x1`,而有的元素动的是 `y1`;
  ③ 只看**最后一个**元素,那个元素恰好静止就误判。
  改成把每个几何元素的一组关键属性拼成签名,并加一条**防空跑**断言:
  签名不能全是空的。顺带发现 `SeriesPreview` 把上下边界写反,
  每帧报一次 `<rect> height: A negative value is not valid`。
  另外把课程标题的等待从"睡 500ms"改成**轮询到标题真的换掉** ——
  课程一多,lazy chunk 的那一瞬就会超过固定睡眠,报出根本不存在的不一致。

  ⚠️ 变异测试脚本又一次跑到一半被杀,`binomial.ts` 带着变异留在盘上。
  脚本开头的 `.bak` 恢复救回来了,但**批次要切小** —— 一次跑二十个变异
  在这台机器上跑不完一个窗口。
